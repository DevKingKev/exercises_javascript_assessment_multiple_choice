module.exports = {
    "metadata": {
        "title": "Advanced HTML5 APIs and Semantic Web Technologies",
        "description": "Test your understanding of advanced HTML5 APIs, semantic markup, web components, and modern web standards implementation.",
        "difficulty": "medium",
        "domain": "html",
        "timeLimit": 45,
        "questionCount": 25,
        "topics": [
            "Semantic HTML5 Elements",
            "Web Components and Custom Elements",
            "HTML5 APIs (Canvas, Geolocation, Storage)",
            "Accessibility (ARIA, Screen Readers)",
            "Microdata and Structured Data",
            "Shadow DOM and Encapsulation",
            "Form Validation and Constraints",
            "Media Elements and APIs",
            "Performance Optimization",
            "Progressive Web App Features"
        ],
        "topicLinks": [
            {
                "topicName": "Semantic HTML5 Elements",
                "refLink": "https://developer.mozilla.org/en-US/docs/Web/HTML/Element#content_sectioning"
            },
            {
                "topicName": "Web Components and Custom Elements",
                "refLink": "https://developer.mozilla.org/en-US/docs/Web/Web_Components"
            },
            {
                "topicName": "HTML5 APIs (Canvas, Geolocation, Storage)",
                "refLink": "https://developer.mozilla.org/en-US/docs/Web/HTML"
            },
            {
                "topicName": "Accessibility (ARIA, Screen Readers)",
                "refLink": "https://developer.mozilla.org/en-US/docs/Web/Accessibility/ARIA"
            },
            {
                "topicName": "Microdata and Structured Data",
                "refLink": "https://developer.mozilla.org/en-US/docs/Web/HTML/Microdata"
            },
            {
                "topicName": "Shadow DOM and Encapsulation",
                "refLink": "https://developer.mozilla.org/en-US/docs/Web/Web_Components/Using_shadow_DOM"
            },
            {
                "topicName": "Form Validation and Constraints",
                "refLink": "https://developer.mozilla.org/en-US/docs/Web/HTML/Constraint_validation"
            },
            {
                "topicName": "Media Elements and APIs",
                "refLink": "https://developer.mozilla.org/en-US/docs/Web/HTML/Element#multimedia"
            },
            {
                "topicName": "Performance Optimization",
                "refLink": "https://developer.mozilla.org/en-US/docs/Web/Performance"
            },
            {
                "topicName": "Progressive Web App Features",
                "refLink": "https://developer.mozilla.org/en-US/docs/Web/Progressive_web_apps"
            }
        ],
        "assessmentId": 1,
        "assessmentUniqueId": 1762947267389,
        "testType": "multiple-choice"
    },
    "questions": [
        {
            "id": 1,
            "question": "What is the key advantage of using the Shadow DOM in web components?\n[CODE]class UserCard extends HTMLElement {\n  constructor() {\n    super();\n    this.attachShadow({ mode: 'open' });\n    this.shadowRoot.innerHTML = `\n      <style>\n        .card { \n          border: 1px solid #ccc; \n          padding: 16px; \n          border-radius: 8px; \n        }\n      </style>\n      <div class=\"card\">\n        <slot name=\"username\"></slot>\n        <slot name=\"email\"></slot>\n      </div>\n    `;\n  }\n}\n\ncustomElements.define('user-card', UserCard);[/CODE]",
            "options": {
                "A": "It makes components load faster",
                "B": "It provides style and DOM encapsulation",
                "C": "It automatically makes components accessible",
                "D": "It reduces JavaScript bundle size"
            },
            "correct": "B",
            "explanation": "The Shadow DOM provides style and DOM encapsulation, preventing CSS leaks and DOM manipulation from outside the component. This means styles defined inside the shadow DOM don't affect the main document, and external styles don't affect the component's internal structure.\n\n[CODE]// Without Shadow DOM - styles leak\n<style>\n.card { color: red; } /* Affects ALL .card elements */\n</style>\n\n// With Shadow DOM - styles are scoped\nclass EncapsulatedComponent extends HTMLElement {\n  constructor() {\n    super();\n    const shadow = this.attachShadow({mode: 'open'});\n    shadow.innerHTML = `\n      <style>\n        .internal { color: blue; } /* Only affects this component */\n      </style>\n      <div class=\"internal\">I'm blue inside</div>\n    `;\n  }\n}\n\n// External CSS cannot affect shadow DOM content\n// JavaScript querySelectors from outside cannot access shadow DOM\n// This provides true encapsulation similar to iframes but lighter[/CODE]\n\nThe Shadow DOM is crucial for building reusable web components that won't break when integrated into different applications with varying CSS frameworks.",
            "topic": {
                "topics": ["Shadow DOM and Encapsulation", "Web Components and Custom Elements"]
            }
        },
        {
            "id": 2,
            "question": "What accessibility issue exists in this ARIA implementation?\n[CODE]<div class=\"tab-panel\">\n  <div role=\"tablist\">\n    <button role=\"tab\" aria-selected=\"true\" aria-controls=\"panel-1\">\n      Tab 1\n    </button>\n    <button role=\"tab\" aria-selected=\"false\" aria-controls=\"panel-2\">\n      Tab 2\n    </button>\n  </div>\n  <div id=\"panel-1\" role=\"tabpanel\" aria-labelledby=\"tab-1\">\n    Content for tab 1\n  </div>\n  <div id=\"panel-2\" role=\"tabpanel\" aria-labelledby=\"tab-2\" hidden>\n    Content for tab 2\n  </div>\n</div>[/CODE]",
            "options": {
                "A": "Missing tabindex attributes",
                "B": "Incorrect aria-labelledby references",
                "C": "No keyboard navigation support",
                "D": "All of the above"
            },
            "correct": "B",
            "explanation": "The main issue is incorrect <pre>aria-labelledby</pre> references. The tabs don't have <pre>id</pre> attributes that match the <pre>aria-labelledby</pre> values in the tabpanels. This breaks the connection between tabs and their content for screen readers.\n\n[CODE]// Corrected implementation\n<div class=\"tab-panel\">\n  <div role=\"tablist\">\n    <button \n      id=\"tab-1\" \n      role=\"tab\" \n      aria-selected=\"true\" \n      aria-controls=\"panel-1\"\n      tabindex=\"0\"> <!-- Added tabindex for focus management -->\n      Tab 1\n    </button>\n    <button \n      id=\"tab-2\" \n      role=\"tab\" \n      aria-selected=\"false\" \n      aria-controls=\"panel-2\"\n      tabindex=\"-1\"> <!-- Non-selected tabs should be tabindex=\"-1\" -->\n      Tab 2\n    </button>\n  </div>\n  <div \n    id=\"panel-1\" \n    role=\"tabpanel\" \n    aria-labelledby=\"tab-1\" \n    tabindex=\"0\"> <!-- Tabpanels should be focusable -->\n    Content for tab 1\n  </div>\n  <div \n    id=\"panel-2\" \n    role=\"tabpanel\" \n    aria-labelledby=\"tab-2\" \n    hidden \n    tabindex=\"0\">\n    Content for tab 2\n  </div>\n</div>\n\n// JavaScript for keyboard navigation\nconst tabs = document.querySelectorAll('[role=\"tab\"]');\ntabs.forEach(tab => {\n  tab.addEventListener('keydown', (e) => {\n    if (e.key === 'ArrowRight' || e.key === 'ArrowLeft') {\n      // Handle arrow key navigation between tabs\n    }\n  });\n});[/CODE]\n\nProper ARIA implementation requires precise ID references and comprehensive keyboard support to be truly accessible.",
            "topic": {
                "topics": ["Accessibility (ARIA, Screen Readers)", "Semantic HTML5 Elements"]
            }
        },
        {
            "id": 3,
            "question": "What is the purpose of the <pre>itemprop</pre> and <pre>itemscope</pre> attributes in this markup?\n[CODE]<div itemscope itemtype=\"https://schema.org/Product\">\n  <h1 itemprop=\"name\">Professional Camera</h1>\n  <img itemprop=\"image\" src=\"camera.jpg\" alt=\"Professional Camera\">\n  <div itemprop=\"offers\" itemscope itemtype=\"https://schema.org/Offer\">\n    <span itemprop=\"price\">499.99</span>\n    <span itemprop=\"priceCurrency\">USD</span>\n  </div>\n  <div itemprop=\"aggregateRating\" itemscope itemtype=\"https://schema.org/AggregateRating\">\n    <span itemprop=\"ratingValue\">4.5</span> stars based on \n    <span itemprop=\"reviewCount\">120</span> reviews\n  </div>\n</div>[/CODE]",
            "options": {
                "A": "To improve CSS styling",
                "B": "To provide structured data for search engines",
                "C": "To enable JavaScript functionality",
                "D": "To validate HTML syntax"
            },
            "correct": "B",
            "explanation": "The <pre>itemprop</pre> and <pre>itemscope</pre> attributes implement microdata to provide structured data for search engines and other applications. This helps search engines understand the content and display rich snippets in search results.\n\n[CODE]// Equivalent JSON-LD representation (alternative approach)\n<script type=\"application/ld+json\">\n{\n  \"@context\": \"https://schema.org/\",\n  \"@type\": \"Product\",\n  \"name\": \"Professional Camera\",\n  \"image\": \"camera.jpg\",\n  \"offers\": {\n    \"@type\": \"Offer\",\n    \"price\": \"499.99\",\n    \"priceCurrency\": \"USD\"\n  },\n  \"aggregateRating\": {\n    \"@type\": \"AggregateRating\",\n    \"ratingValue\": \"4.5\",\n    \"reviewCount\": \"120\"\n  }\n}\n</script>\n\n// Benefits of structured data:\n// - Rich snippets in search results (stars, prices, availability)\n// - Better understanding of page content by search engines\n// - Enhanced display in social media shares\n// - Voice search optimization\n\n// Common schema.org types:\n// - Article, BlogPosting\n// - LocalBusiness, Restaurant\n// - Event, Movie\n// - Person, Organization\n// - Product, Offer\n\n// Extracting microdata with JavaScript\nconst product = document.querySelector('[itemtype=\"https://schema.org/Product\"]');\nif (product) {\n  const name = product.querySelector('[itemprop=\"name\"]').textContent;\n  const price = product.querySelector('[itemprop=\"price\"]').textContent;\n  console.log(`Product: ${name}, Price: $${price}`);\n}[/CODE]\n\nStructured data is essential for SEO and helps your content stand out in search results with enhanced displays.",
            "topic": {
                "topics": ["Microdata and Structured Data", "Semantic HTML5 Elements"]
            }
        },
        {
            "id": 4,
            "question": "What performance optimization technique is demonstrated in this <pre>picture</pre> element?\n[CODE]<picture>\n  <source \n    media=\"(min-width: 1200px)\"\n    srcset=\"hero-large.jpg 1x, hero-large@2x.jpg 2x\"\n    type=\"image/jpeg\">\n  <source \n    media=\"(min-width: 768px)\" \n    srcset=\"hero-medium.jpg 1x, hero-medium@2x.jpg 2x\"\n    type=\"image/jpeg\">\n  <source \n    srcset=\"hero-small.jpg 1x, hero-small@2x.jpg 2x\"\n    type=\"image/jpeg\">\n  <img \n    src=\"hero-small.jpg\" \n    alt=\"Hero image\" \n    loading=\"lazy\"\n    width=\"800\" \n    height=\"400\">\n</picture>[/CODE]",
            "options": {
                "A": "Progressive image loading",
                "B": "Responsive images with art direction",
                "C": "Image compression optimization",
                "D": "CSS sprite generation"
            },
            "correct": "B",
            "explanation": "This demonstrates responsive images with art direction using the <pre>picture</pre> element. Different images are served based on viewport size and pixel density, ensuring optimal image quality and file size for each device.\n\n[CODE]// Art direction vs resolution switching\n// Art direction: Different crops/compositions for different viewports\n<picture>\n  <source media=\"(min-width: 1200px)\" srcset=\"hero-desktop.jpg\">\n  <source media=\"(min-width: 768px)\" srcset=\"hero-tablet.jpg\">\n  <img src=\"hero-mobile.jpg\" alt=\"Hero\">\n</picture>\n\n// Resolution switching: Same image, different resolutions\n<img \n  srcset=\"image-320w.jpg 320w,\n          image-640w.jpg 640w,\n          image-1200w.jpg 1200w\"\n  sizes=\"(max-width: 600px) 100vw,\n         (max-width: 1200px) 50vw,\n         33vw\"\n  src=\"image-640w.jpg\" \n  alt=\"Responsive image\">\n\n// Additional performance optimizations\n<img \n  src=\"image.jpg\" \n  alt=\"Optimized image\"\n  loading=\"lazy\"           // Defer offscreen images\n  decoding=\"async\"         // Non-blocking image decoding\n  width=\"800\"              // Prevent layout shifts\n  height=\"400\"\n  fetchpriority=\"high\">    // Priority hint for important images\n\n// Modern format support with fallback\n<picture>\n  <source srcset=\"image.avif\" type=\"image/avif\">\n  <source srcset=\"image.webp\" type=\"image/webp\">\n  <img src=\"image.jpg\" alt=\"Modern formats with fallback\">\n</picture>[/CODE]\n\nResponsive images significantly improve performance by ensuring devices only download appropriately sized images, reducing bandwidth usage and improving load times.",
            "topic": {
                "topics": ["Performance Optimization", "Media Elements and APIs"]
            }
        },
        {
            "id": 5,
            "question": "What is the main security concern with this HTML5 storage implementation?\n[CODE]// Storing user data\nlocalStorage.setItem('userSession', JSON.stringify({\n  userId: 12345,\n  username: 'john_doe',\n  email: 'john@example.com',\n  authToken: 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...',\n  permissions: ['read', 'write', 'admin']\n}));\n\n// Retrieving data\nconst session = JSON.parse(localStorage.getItem('userSession'));\nif (session && session.authToken) {\n  fetch('/api/user/data', {\n    headers: {\n      'Authorization': `Bearer ${session.authToken}`\n    }\n  });\n}[/CODE]",
            "options": {
                "A": "LocalStorage size limitations",
                "B": "XSS vulnerability exposing sensitive data",
                "C": "No data encryption",
                "D": "Both B and C"
            },
            "correct": "D",
            "explanation": "The main security concerns are XSS vulnerability exposing sensitive data and lack of encryption. LocalStorage is accessible by any JavaScript running on the same domain, making authentication tokens and sensitive user data vulnerable to XSS attacks.\n\n[CODE]// Secure alternative using HttpOnly cookies\n// Backend sets secure cookie\nresponse.setHeader('Set-Cookie', [\n  `authToken=eyJhbGci...; HttpOnly; Secure; SameSite=Strict`,\n  `userId=12345; HttpOnly; Secure; SameSite=Strict`\n]);\n\n// Frontend - only store non-sensitive data in localStorage\nlocalStorage.setItem('userPreferences', JSON.stringify({\n  theme: 'dark',\n  language: 'en',\n  notifications: true\n}));\n\n// Authentication token is automatically sent with requests\nfetch('/api/user/data'); // Cookie with authToken is automatically included\n\n// For sensitive data, use sessionStorage (cleared on tab close)\nsessionStorage.setItem('temporaryData', JSON.stringify({\n  formData: { /* sensitive but temporary data */ }\n}));\n\n// Additional security measures\n// 1. Content Security Policy (CSP)\n<meta http-equiv=\"Content-Security-Policy\" \n      content=\"default-src 'self'; script-src 'self' 'unsafe-inline';\">\n\n// 2. Input sanitization\nfunction sanitizeHTML(str) {\n  const div = document.createElement('div');\n  div.textContent = str;\n  return div.innerHTML;\n}\n\n// 3. HTTPS enforcement\n<meta http-equiv=\"Strict-Transport-Security\" \n      content=\"max-age=31536000; includeSubDomains\">[/CODE]\n\nNever store authentication tokens or sensitive user data in localStorage. Use HttpOnly cookies for authentication and reserve localStorage for non-sensitive preferences.",
            "topic": {
                "topics": ["HTML5 APIs (Canvas, Geolocation, Storage)", "Performance Optimization"]
            }
        },
        {
            "id": 6,
            "question": "What accessibility feature is missing from this custom video player?\n[CODE]<div class=\"video-player\">\n  <video \n    id=\"main-video\"\n    src=\"presentation.mp4\"\n    aria-label=\"Product presentation video\"\n    controls>\n    <track \n      kind=\"captions\" \n      src=\"captions.vtt\" \n      srclang=\"en\" \n      label=\"English\">\n    <track \n      kind=\"chapters\" \n      src=\"chapters.vtt\" \n      srclang=\"en\">\n  </video>\n  <div class=\"custom-controls\">\n    <button onclick=\"togglePlay()\">Play/Pause</button>\n    <input type=\"range\" onchange=\"setVolume(this.value)\" min=\"0\" max=\"1\" step=\"0.1\">\n    <button onclick=\"toggleMute()\">Mute</button>\n  </div>\n</div>[/CODE]",
            "options": {
                "A": "Keyboard navigation support",
                "B": "Screen reader announcements",
                "C": "High contrast mode support",
                "D": "All of the above"
            },
            "correct": "D",
            "explanation": "The custom video player is missing several key accessibility features: keyboard navigation support, proper screen reader announcements, and high contrast mode compatibility. While the native video element has some built-in accessibility, custom controls need explicit implementation.\n\n[CODE]// Accessible custom video player implementation\n<div class=\"video-player\" role=\"application\" aria-label=\"Video player\">\n  <video \n    id=\"main-video\"\n    src=\"presentation.mp4\"\n    aria-labelledby=\"video-title\"\n    aria-describedby=\"video-desc\">\n    <track kind=\"captions\" src=\"captions.vtt\" srclang=\"en\" label=\"English\" default>\n    <track kind=\"descriptions\" src=\"descriptions.vtt\" srclang=\"en\">\n  </video>\n  \n  <div class=\"custom-controls\">\n    <button \n      id=\"play-btn\"\n      onclick=\"togglePlay()\" \n      aria-label=\"Play video\"\n      aria-keyshortcuts=\"Space K\"\n      tabindex=\"0\">\n      <span aria-hidden=\"true\">▶</span>\n      <span class=\"sr-only\">Play</span>\n    </button>\n    \n    <input \n      type=\"range\" \n      id=\"volume-slider\"\n      onchange=\"setVolume(this.value)\" \n      min=\"0\" max=\"1\" step=\"0.1\"\n      aria-label=\"Volume control\"\n      aria-valuetext=\"${value} percent\"\n      tabindex=\"0\">\n    \n    <button \n      onclick=\"toggleMute()\"\n      aria-label=\"Mute audio\"\n      aria-keyshortcuts=\"M\"\n      tabindex=\"0\">\n      <span aria-hidden=\"true\">🔇</span>\n    </button>\n    \n    <button \n      onclick=\"toggleCaptions()\"\n      aria-label=\"Toggle captions\"\n      aria-keyshortcuts=\"C\"\n      tabindex=\"0\">\n      <span aria-hidden=\"true\">CC</span>\n    </button>\n  </div>\n  \n  <div id=\"video-title\" class=\"sr-only\">Product Presentation</div>\n  <div id=\"video-desc\" class=\"sr-only\">\n    A 5-minute demonstration of our new product features\n  </div>\n</div>\n\n// Keyboard event handling\ndocument.addEventListener('keydown', (e) => {\n  if (e.target.closest('.video-player')) {\n    switch(e.key) {\n      case ' ': // Space\n      case 'k': // K key\n        e.preventDefault();\n        togglePlay();\n        break;\n      case 'm': // M key\n        toggleMute();\n        break;\n      case 'c': // C key\n        toggleCaptions();\n        break;\n    }\n  }\n});\n\n// Screen reader announcements\nfunction announceToScreenReader(message) {\n  const announcer = document.getElementById('aria-live-announcer') || \n    createAriaLiveRegion();\n  announcer.textContent = message;\n}\n\nfunction createAriaLiveRegion() {\n  const div = document.createElement('div');\n  div.id = 'aria-live-announcer';\n  div.setAttribute('aria-live', 'polite');\n  div.setAttribute('aria-atomic', 'true');\n  div.className = 'sr-only';\n  document.body.appendChild(div);\n  return div;\n}[/CODE]\n\nCustom media controls must implement full keyboard navigation, ARIA attributes, and screen reader support to be truly accessible.",
            "topic": {
                "topics": ["Accessibility (ARIA, Screen Readers)", "Media Elements and APIs"]
            }
        },
        {
            "id": 7,
            "question": "What is the purpose of the <pre>template</pre> element in this web component?\n[CODE]<template id=\"user-card-template\">\n  <style>\n    .user-card {\n      border: 1px solid #ddd;\n      border-radius: 8px;\n      padding: 16px;\n      margin: 8px;\n    }\n    .user-name {\n      font-weight: bold;\n      color: #333;\n    }\n  </style>\n  <div class=\"user-card\">\n    <div class=\"user-name\"></div>\n    <div class=\"user-email\"></div>\n    <button class=\"action-btn\">Contact</button>\n  </div>\n</template>\n\n<script>\nclass UserCard extends HTMLElement {\n  constructor() {\n    super();\n    const template = document.getElementById('user-card-template');\n    const content = template.content.cloneNode(true);\n    \n    content.querySelector('.user-name').textContent = this.getAttribute('name');\n    content.querySelector('.user-email').textContent = this.getAttribute('email');\n    \n    this.attachShadow({ mode: 'open' }).appendChild(content);\n  }\n}\n\ncustomElements.define('user-card', UserCard);\n</script>[/CODE]",
            "options": {
                "A": "To improve SEO performance",
                "B": "To define reusable markup that isn't rendered until activated",
                "C": "To create CSS styles that only apply to specific elements",
                "D": "To store JavaScript functions for later use"
            },
            "correct": "B",
            "explanation": "The <pre>template</pre> element defines reusable markup that isn't rendered until activated. Content inside a template is inert - it doesn't load images, run scripts, or affect the document until it's cloned and inserted into the DOM.\n\n[CODE]// Template element characteristics:\n// - Content is not rendered by default\n// - Not in the main document flow\n// - Can be accessed and cloned via JavaScript\n// - Ideal for reusable component templates\n\n// Multiple ways to use templates:\n\n// 1. Declarative approach (shown in question)\n<template id=\"my-template\">\n  <div class=\"component\">\n    <h2>Title</h2>\n    <p>Content</p>\n  </div>\n</template>\n\n// 2. Programmatic approach\nconst template = document.createElement('template');\ntemplate.innerHTML = `\n  <style>\n    .dynamic { color: blue; }\n  </style>\n  <div class=\"dynamic\">\n    <slot></slot>\n  </div>\n`;\n\nclass DynamicElement extends HTMLElement {\n  constructor() {\n    super();\n    this.attachShadow({mode: 'open'})\n        .appendChild(template.content.cloneNode(true));\n  }\n}\n\n// 3. Template with data binding\nfunction createUserList(users) {\n  const template = document.getElementById('user-item-template');\n  const fragment = document.createDocumentFragment();\n  \n  users.forEach(user => {\n    const instance = template.content.cloneNode(true);\n    instance.querySelector('.name').textContent = user.name;\n    instance.querySelector('.email').textContent = user.email;\n    instance.querySelector('.avatar').src = user.avatarUrl;\n    fragment.appendChild(instance);\n  });\n  \n  return fragment;\n}\n\n// 4. Conditional templates\n<template id=\"logged-in\">\n  <div>Welcome back, <span class=\"username\"></span></div>\n</template>\n\n<template id=\"logged-out\">\n  <div>Please log in</div>\n</template>\n\nfunction showAppropriateView(isLoggedIn, username) {\n  const templateId = isLoggedIn ? 'logged-in' : 'logged-out';\n  const template = document.getElementById(templateId);\n  const content = template.content.cloneNode(true);\n  \n  if (isLoggedIn) {\n    content.querySelector('.username').textContent = username;\n  }\n  \n  document.getElementById('app').innerHTML = '';\n  document.getElementById('app').appendChild(content);\n}[/CODE]\n\nTemplates are essential for efficient DOM manipulation, web components, and dynamic content generation without affecting initial page rendering.",
            "topic": {
                "topics": ["Web Components and Custom Elements", "Performance Optimization"]
            }
        },
        {
            "id": 8,
            "question": "What problem does this Service Worker registration solve?\n[CODE]// service-worker.js\nconst CACHE_NAME = 'app-v1.2.0';\nconst urlsToCache = [\n  '/',\n  '/styles/main.css',\n  '/scripts/app.js',\n  '/images/logo.svg'\n];\n\nself.addEventListener('install', event => {\n  event.waitUntil(\n    caches.open(CACHE_NAME)\n      .then(cache => cache.addAll(urlsToCache))\n  );\n});\n\nself.addEventListener('fetch', event => {\n  event.respondWith(\n    caches.match(event.request)\n      .then(response => {\n        if (response) {\n          return response; // Return cached version\n        }\n        return fetch(event.request); // Fall back to network\n      })\n  );\n});\n\n// Registration in main app\nif ('serviceWorker' in navigator) {\n  navigator.serviceWorker.register('/service-worker.js')\n    .then(registration => {\n      console.log('SW registered: ', registration);\n    })\n    .catch(registrationError => {\n      console.log('SW registration failed: ', registrationError);\n    });\n}[/CODE]",
            "options": {
                "A": "It enables offline functionality and faster loading",
                "B": "It improves SEO ranking",
                "C": "It automatically compresses images",
                "D": "It enables real-time collaboration"
            },
            "correct": "A",
            "explanation": "This Service Worker enables offline functionality and faster loading by caching critical resources. Users can access the app even without internet connection, and cached resources load instantly on repeat visits.\n\n[CODE]// Enhanced Service Worker with strategies\nconst CACHE_NAME = 'app-v1.2.0';\nconst API_CACHE = 'api-v1';\n\n// Cache strategies:\n\n// 1. Cache First (for static assets)\nself.addEventListener('fetch', event => {\n  if (event.request.url.includes('/static/')) {\n    event.respondWith(\n      caches.match(event.request)\n        .then(cached => cached || fetch(event.request))\n    );\n  }\n  \n  // 2. Network First (for API calls)\n  else if (event.request.url.includes('/api/')) {\n    event.respondWith(\n      fetch(event.request)\n        .then(networkResponse => {\n          const responseClone = networkResponse.clone();\n          caches.open(API_CACHE)\n            .then(cache => cache.put(event.request, responseClone));\n          return networkResponse;\n        })\n        .catch(() => caches.match(event.request))\n    );\n  }\n  \n  // 3. Stale-While-Revalidate (for frequently updated content)\n  else {\n    event.respondWith(\n      caches.match(event.request)\n        .then(cached => {\n          const fetchPromise = fetch(event.request)\n            .then(networkResponse => {\n              caches.open(CACHE_NAME)\n                .then(cache => cache.put(event.request, networkResponse));\n              return networkResponse;\n            });\n          return cached || fetchPromise;\n        })\n    );\n  }\n});\n\n// Cache cleanup on activation\nself.addEventListener('activate', event => {\n  event.waitUntil(\n    caches.keys().then(cacheNames => {\n      return Promise.all(\n        cacheNames.map(cacheName => {\n          if (cacheName !== CACHE_NAME && cacheName !== API_CACHE) {\n            return caches.delete(cacheName); // Remove old caches\n          }\n        })\n      );\n    })\n  );\n});\n\n// Background sync for offline actions\nself.addEventListener('sync', event => {\n  if (event.tag === 'background-sync') {\n    event.waitUntil(doBackgroundSync());\n  }\n});\n\n// Push notifications\nself.addEventListener('push', event => {\n  const options = {\n    body: event.data.text(),\n    icon: '/images/icon-192.png',\n    badge: '/images/badge-72.png'\n  };\n  \n  event.waitUntil(\n    self.registration.showNotification('App Notification', options)\n  );\n});[/CODE]\n\nService Workers are fundamental for Progressive Web Apps (PWAs), enabling reliable performance, offline functionality, and native-app-like features.",
            "topic": {
                "topics": ["Progressive Web App Features", "Performance Optimization"]
            }
        },
        {
            "id": 9,
            "question": "What is the significance of the <pre>loading</pre> attribute in this image optimization?\n[CODE]<img \n  src=\"hero-image.jpg\"\n  alt=\"Company headquarters\"\n  loading=\"lazy\"\n  width=\"1200\"\n  height=\"600\"\n  decoding=\"async\"\n  fetchpriority=\"high\">\n\n<img \n  src=\"user-avatar.jpg\" \n  alt=\"User profile picture\"\n  loading=\"lazy\"\n  width=\"100\"\n  height=\"100\"\n  decoding=\"async\">\n\n<img \n  src=\"below-fold-image.jpg\"\n  alt=\"Additional content\"\n  loading=\"lazy\"\n  width=\"800\"\n  height=\"400\">[/CODE]",
            "options": {
                "A": "It enables progressive JPEG loading",
                "B": "It defers offscreen image loading until needed",
                "C": "It automatically compresses images",
                "D": "It enables responsive image behavior"
            },
            "correct": "B",
            "explanation": "The <pre>loading=\"lazy\"</pre> attribute defers offscreen image loading until they're about to enter the viewport. This significantly improves initial page load performance by reducing network requests and bandwidth usage.\n\n[CODE]// Lazy loading strategies\n\n// 1. Native lazy loading (modern browsers)\n<img \n  src=\"image.jpg\" \n  loading=\"lazy\" \n  alt=\"Lazy loaded\"\n  width=\"800\" height=\"400\">\n\n// 2. Eager loading (above the fold, important images)\n<img \n  src=\"hero.jpg\" \n  loading=\"eager\" \n  alt=\"Important hero image\"\n  fetchpriority=\"high\"\n  width=\"1200\" height=\"600\">\n\n// 3. Auto (browser decides)\n<img \n  src=\"image.jpg\" \n  loading=\"auto\" \n  alt=\"Browser decides\"\n  width=\"400\" height=\"300\">\n\n// Additional optimization attributes\n<img \n  src=\"image.jpg\"\n  alt=\"Fully optimized\"\n  loading=\"lazy\"           // Defer offscreen images\n  decoding=\"async\"         // Non-blocking decoding\n  width=\"800\"              // Prevent layout shifts\n  height=\"400\"\n  fetchpriority=\"low\"      // Low priority hint\n  sizes=\"(max-width: 600px) 100vw, 50vw\"\n  srcset=\"image-400.jpg 400w, image-800.jpg 800w\">\n\n// Lazy loading with Intersection Observer (fallback)\nfunction lazyLoadImages() {\n  const images = document.querySelectorAll('img[data-src]');\n  \n  const observer = new IntersectionObserver((entries) => {\n    entries.forEach(entry => {\n      if (entry.isIntersecting) {\n        const img = entry.target;\n        img.src = img.dataset.src;\n        img.removeAttribute('data-src');\n        observer.unobserve(img);\n      }\n    });\n  });\n  \n  images.forEach(img => observer.observe(img));\n}\n\n// Usage for fallback\n<img \n  data-src=\"image.jpg\" \n  src=\"placeholder.jpg\" \n  alt=\"Lazy loaded with fallback\"\n  width=\"800\" height=\"400\">\n\n// Lazy loading for background images\n<div \n  class=\"lazy-bg\" \n  data-bg=\"url('background.jpg')\"\n  style=\"width: 100%; height: 400px;\">\n</div>\n\n// CSS for background image lazy loading\n.lazy-bg {\n  background-color: #f0f0f0;\n}\n\n.lazy-bg.loaded {\n  background-image: var(--bg-image);\n}[/CODE]\n\nNative lazy loading provides significant performance benefits with minimal implementation effort, especially for image-heavy pages.",
            "topic": {
                "topics": ["Performance Optimization", "Media Elements and APIs"]
            }
        },
        {
            "id": 10,
            "question": "What accessibility improvement does this ARIA live region provide?\n[CODE]<div \n  id=\"search-results\"\n  role=\"region\"\n  aria-live=\"polite\"\n  aria-atomic=\"true\"\n  aria-label=\"Search results\"\n  aria-busy=\"false\">\n  <!-- Dynamic content will be inserted here -->\n</div>\n\n<script>\nasync function performSearch(query) {\n  const resultsElement = document.getElementById('search-results');\n  \n  // Indicate loading state\n  resultsElement.setAttribute('aria-busy', 'true');\n  resultsElement.innerHTML = '<p>Searching...</p>';\n  \n  try {\n    const response = await fetch(`/api/search?q=${encodeURIComponent(query)}`);\n    const results = await response.json();\n    \n    // Update with results\n    resultsElement.setAttribute('aria-busy', 'false');\n    \n    if (results.length === 0) {\n      resultsElement.innerHTML = '<p>No results found</p>';\n    } else {\n      resultsElement.innerHTML = results.map(result => \n        `<div class=\"result-item\">\n           <h3>${escapeHTML(result.title)}</h3>\n           <p>${escapeHTML(result.description)}</p>\n         </div>`\n      ).join('');\n      \n      // Announce results to screen readers\n      const announcement = `Found ${results.length} search results`;\n      announceToScreenReader(announcement);\n    }\n    \n  } catch (error) {\n    resultsElement.setAttribute('aria-busy', 'false');\n    resultsElement.innerHTML = '<p>Search failed. Please try again.</p>';\n    announceToScreenReader('Search failed');\n  }\n}\n\nfunction announceToScreenReader(message) {\n  const announcer = document.getElementById('aria-announcer') ||\n    createAriaLiveRegion('aria-announcer', 'assertive');\n  announcer.textContent = message;\n}\n</script>[/CODE]",
            "options": {
                "A": "It makes search results visible to screen readers as they update",
                "B": "It improves SEO for search results",
                "C": "It enables voice search functionality",
                "D": "It automatically translates content"
            },
            "correct": "A",
            "explanation": "The ARIA live region makes dynamic content (search results) visible to screen readers as it updates. Screen readers will automatically announce changes to the content within the live region, keeping users informed about search progress and results.\n\n[CODE]// ARIA live region attributes explained:\n\n// aria-live=\"polite\" - announces when user is idle\n// aria-live=\"assertive\" - interrupts for important announcements\n// aria-live=\"off\" - default, no announcements\n\n// aria-atomic=\"true\" - announces entire region content\n// aria-atomic=\"false\" - announces only changed content\n\n// aria-busy=\"true\" - indicates content is updating\n// aria-busy=\"false\" - content is stable\n\n// Common live region patterns:\n\n// 1. Status updates\n<div \n  aria-live=\"polite\" \n  aria-atomic=\"true\"\n  class=\"sr-only\"\n  id=\"status-announcer\">\n</div>\n\n// 2. Form validation messages\n<div \n  aria-live=\"polite\"\n  role=\"alert\"\n  id=\"form-errors\">\n</div>\n\n// 3. Progress indicators\n<div \n  aria-live=\"assertive\" \n  aria-atomic=\"true\"\n  id=\"upload-progress\">\n  Uploading: <span id=\"progress-percent\">0</span>%\n</div>\n\n// 4. Notifications\n<div \n  aria-live=\"polite\"\n  role=\"status\"\n  id=\"notifications\">\n</div>\n\n// Implementation helper functions\nfunction createLiveRegion(id, politeness = 'polite', atomic = true) {\n  let region = document.getElementById(id);\n  if (!region) {\n    region = document.createElement('div');\n    region.id = id;\n    region.setAttribute('aria-live', politeness);\n    region.setAttribute('aria-atomic', atomic.toString());\n    region.className = 'sr-only';\n    document.body.appendChild(region);\n  }\n  return region;\n}\n\nfunction announce(message, politeness = 'polite') {\n  const region = createLiveRegion('global-announcer', politeness);\n  region.textContent = ''; // Clear previous\n  setTimeout(() => {\n    region.textContent = message;\n  }, 100);\n}\n\n// Usage examples\nannounce('Page loaded successfully');\nannounce('Error: Form submission failed', 'assertive');\n\n// Dynamic content best practices\nfunction updateDynamicContent(container, newContent) {\n  const oldContent = container.innerHTML;\n  \n  // For screen readers, sometimes better to completely replace\n  container.innerHTML = '';\n  setTimeout(() => {\n    container.innerHTML = newContent;\n  }, 50);\n  \n  // Or use CSS to hide/show\n  container.style.visibility = 'hidden';\n  container.innerHTML = newContent;\n  container.style.visibility = 'visible';\n}[/CODE]\n\nARIA live regions are essential for making dynamic web applications accessible to screen reader users, providing real-time updates about content changes.",
            "topic": {
                "topics": ["Accessibility (ARIA, Screen Readers)", "Semantic HTML5 Elements"]
            }
        },
        {
            "id": 11,
            "question": "What semantic HTML improvement can be made to this navigation structure?\n[CODE]<div class=\"nav\">\n  <div class=\"nav-header\">Main Navigation</div>\n  <ul>\n    <li><a href=\"/home\">Home</a></li>\n    <li><a href=\"/about\">About Us</a></li>\n    <li>\n      <a href=\"/services\">Services</a>\n      <ul>\n        <li><a href=\"/services/web\">Web Development</a></li>\n        <li><a href=\"/services/mobile\">Mobile Apps</a></li>\n      </ul>\n    </li>\n    <li><a href=\"/contact\">Contact</a></li>\n  </ul>\n</div>[/CODE]",
            "options": {
                "A": "Replace div with nav element and add ARIA labels",
                "B": "Use tables for better navigation structure",
                "C": "Add more CSS classes for styling",
                "D": "Convert to JavaScript-powered navigation"
            },
            "correct": "A",
            "explanation": "The navigation should use the semantic <pre>nav</pre> element instead of a generic <pre>div</pre>, and appropriate ARIA labels should be added for better accessibility and semantic structure.\n\n[CODE]// Improved semantic navigation\n<nav aria-label=\"Main navigation\">\n  <h2 class=\"sr-only\">Main Navigation</h2>\n  <ul>\n    <li><a href=\"/home\" aria-current=\"page\">Home</a></li>\n    <li><a href=\"/about\">About Us</a></li>\n    <li>\n      <a href=\"/services\" aria-expanded=\"false\" aria-haspopup=\"true\">\n        Services\n      </a>\n      <ul aria-label=\"Services submenu\">\n        <li><a href=\"/services/web\">Web Development</a></li>\n        <li><a href=\"/services/mobile\">Mobile Apps</a></li>\n      </ul>\n    </li>\n    <li><a href=\"/contact\">Contact</a></li>\n  </ul>\n</nav>\n\n// Additional semantic improvements for complex sites\n\n// 1. Multiple navigation regions\n<nav aria-label=\"Primary\">\n  <!-- Main site navigation -->\n</nav>\n\n<nav aria-label=\"Secondary\">\n  <!-- Utility links (login, search, etc.) -->\n</nav>\n\n<nav aria-label=\"Breadcrumb\">\n  <ol>\n    <li><a href=\"/\">Home</a></li>\n    <li><a href=\"/products\">Products</a></li>\n    <li aria-current=\"page\">Current Product</li>\n  </ol>\n</nav>\n\n// 2. Skip links for keyboard users\n<a href=\"#main-content\" class=\"skip-link\">Skip to main content</a>\n\n// 3. Landmark roles for complex layouts\n<header role=\"banner\">\n  <!-- Site header content -->\n</header>\n\n<nav role=\"navigation\" aria-label=\"Main\">\n  <!-- Navigation -->\n</nav>\n\n<main role=\"main\" id=\"main-content\">\n  <!-- Main content -->\n</main>\n\n<aside role=\"complementary\" aria-label=\"Related information\">\n  <!-- Sidebar content -->\n</aside>\n\n<footer role=\"contentinfo\">\n  <!-- Footer content -->\n</footer>\n\n// 4. Mobile navigation with proper semantics\n<nav aria-label=\"Mobile navigation\">\n  <button \n    aria-expanded=\"false\" \n    aria-controls=\"mobile-menu\"\n    aria-label=\"Toggle mobile menu\">\n    <span aria-hidden=\"true\">☰</span>\n  </button>\n  \n  <div id=\"mobile-menu\" hidden>\n    <ul>\n      <li><a href=\"/home\">Home</a></li>\n      <!-- Menu items -->\n    </ul>\n  </div>\n</nav>[/CODE]\n\nUsing proper semantic elements and ARIA attributes improves accessibility, SEO, and maintainability of navigation structures.",
            "topic": {
                "topics": ["Semantic HTML5 Elements", "Accessibility (ARIA, Screen Readers)"]
            }
        },
        {
            "id": 12,
            "question": "What is the purpose of the <pre>defer</pre> and <pre>async</pre> attributes in script loading?\n[CODE]<!-- Critical script that depends on DOM -->\n<script src=\"analytics.js\" defer></script>\n\n<!-- Independent third-party script -->\n<script src=\"chat-widget.js\" async></script>\n\n<!-- Traditional blocking script -->\n<script src=\"legacy.js\"></script>[/CODE]",
            "options": {
                "A": "They make scripts load faster",
                "B": "They control when scripts execute relative to HTML parsing",
                "C": "They automatically minify JavaScript code",
                "D": "They enable module functionality"
            },
            "correct": "B",
            "explanation": "The <pre>defer</pre> and <pre>async</pre> attributes control when scripts execute relative to HTML parsing, affecting page load performance and execution order.\n\n[CODE]// Script loading behavior comparison:\n\n// 1. Traditional (blocking)\n<script src=\"script.js\"></script>\n// - HTML parsing pauses\n// - Script downloads and executes immediately\n// - HTML parsing resumes after execution\n\n// 2. Async (non-blocking, execution order not guaranteed)\n<script src=\"script.js\" async></script>\n// - HTML parsing continues\n// - Script downloads in parallel\n// - Script executes immediately after download, pausing HTML parsing\n// - Multiple async scripts execute in download order, not source order\n\n// 3. Defer (non-blocking, execution order preserved)\n<script src=\"script.js\" defer></script>\n// - HTML parsing continues\n// - Script downloads in parallel\n// - Script executes after HTML parsing completes, before DOMContentLoaded\n// - Multiple defer scripts execute in source order\n\n// Practical usage guidelines:\n\n// Use defer for scripts that need the DOM but aren't time-critical\n<script src=\"main-app.js\" defer></script>\n<script src=\"analytics.js\" defer></script>\n\n// Use async for independent third-party scripts\n<script src=\"google-analytics.js\" async></script>\n<script src=\"chat-widget.js\" async></script>\n\n// Use module type for modern ES6 modules (defer by default)\n<script type=\"module\" src=\"app.js\"></script>\n\n// Modern performance optimization pattern\n// Critical scripts in head with defer\n<head>\n  <script src=\"critical.js\" defer></script>\n</head>\n\n// Non-critical scripts at end of body\n<body>\n  <!-- content -->\n  <script src=\"non-critical.js\" async></script>\n</body>\n\n// Dynamic script loading\nfunction loadScript(src, async = true) {\n  const script = document.createElement('script');\n  script.src = src;\n  script.async = async;\n  document.head.appendChild(script);\n}\n\n// Preload important scripts\n<link rel=\"preload\" href=\"important.js\" as=\"script\">\n\n// Module preloading\n<link rel=\"modulepreload\" href=\"main-module.js\">[/CODE]\n\nUnderstanding script loading attributes is crucial for optimizing page load performance and ensuring proper execution order of dependencies.",
            "topic": {
                "topics": ["Performance Optimization", "HTML5 APIs (Canvas, Geolocation, Storage)"]
            }
        },
        {
            "id": 13,
            "question": "What accessibility issue exists in this form implementation?\n[CODE]<form>\n  <div>\n    <span>Email Address</span>\n    <input type=\"email\" name=\"email\">\n  </div>\n  \n  <div>\n    <span>Password</span>\n    <input type=\"password\" name=\"password\">\n  </div>\n  \n  <div>\n    <input type=\"checkbox\" name=\"remember\">\n    <span>Remember me</span>\n  </div>\n  \n  <button>Sign In</button>\n</form>[/CODE]",
            "options": {
                "A": "Missing form labels and associations",
                "B": "No form validation",
                "C": "Incorrect input types",
                "D": "Missing CSRF protection"
            },
            "correct": "A",
            "explanation": "The main accessibility issue is missing proper form labels and associations. Screen reader users cannot understand the purpose of form fields without proper <pre>label</pre> elements associated with inputs.\n\n[CODE]// Accessible form implementation\n<form>\n  <div>\n    <label for=\"email\">Email Address</label>\n    <input \n      type=\"email\" \n      id=\"email\" \n      name=\"email\"\n      required\n      aria-required=\"true\"\n      autocomplete=\"email\">\n  </div>\n  \n  <div>\n    <label for=\"password\">Password</label>\n    <input \n      type=\"password\" \n      id=\"password\" \n      name=\"password\"\n      required\n      aria-required=\"true\"\n      autocomplete=\"current-password\"\n      minlength=\"8\"\n      aria-describedby=\"password-help\">\n    <div id=\"password-help\" class=\"help-text\">\n      Password must be at least 8 characters\n    </div>\n  </div>\n  \n  <div>\n    <input \n      type=\"checkbox\" \n      id=\"remember\" \n      name=\"remember\">\n    <label for=\"remember\">Remember me</label>\n  </div>\n  \n  <button type=\"submit\">Sign In</button>\n</form>\n\n// Enhanced form with additional accessibility features\n\n// 1. Fieldset and legend for grouped controls\n<fieldset>\n  <legend>Payment Information</legend>\n  \n  <div>\n    <label for=\"card-number\">Card Number</label>\n    <input \n      type=\"text\" \n      id=\"card-number\" \n      name=\"cardNumber\"\n      inputmode=\"numeric\"\n      pattern=\"[0-9\\s]+\"\n      aria-describedby=\"card-format\">\n    <div id=\"card-format\" class=\"sr-only\">\n      Enter 16-digit card number with spaces\n    </div>\n  </div>\n  \n  <!-- More payment fields -->\n</fieldset>\n\n// 2. Error handling with ARIA\n<div>\n  <label for=\"username\">Username</label>\n  <input \n    type=\"text\" \n    id=\"username\" \n    name=\"username\"\n    aria-invalid=\"false\"\n    aria-describedby=\"username-error\">\n  <div \n    id=\"username-error\" \n    class=\"error-message\" \n    role=\"alert\"\n    aria-live=\"polite\">\n  </div>\n</div>\n\n// 3. Required field indicators\n<label for=\"email\">\n  Email Address \n  <span class=\"required\" aria-hidden=\"true\">*</span>\n  <span class=\"sr-only\">required</span>\n</label>\n\n// 4. Form instructions\n<div role=\"region\" aria-labelledby=\"form-instructions\">\n  <h3 id=\"form-instructions\" class=\"sr-only\">Form Instructions</h3>\n  <p>All fields marked with an asterisk (*) are required.</p>\n</div>\n\n// JavaScript for dynamic error handling\nfunction showError(fieldId, message) {\n  const field = document.getElementById(fieldId);\n  const errorElement = document.getElementById(fieldId + '-error');\n  \n  field.setAttribute('aria-invalid', 'true');\n  errorElement.textContent = message;\n  \n  // Focus management for screen readers\n  field.focus();\n}\n\nfunction clearError(fieldId) {\n  const field = document.getElementById(fieldId);\n  const errorElement = document.getElementById(fieldId + '-error');\n  \n  field.setAttribute('aria-invalid', 'false');\n  errorElement.textContent = '';\n}[/CODE]\n\nProper form labeling and ARIA attributes are essential for making forms accessible to screen reader users and people with disabilities.",
            "topic": {
                "topics": ["Accessibility (ARIA, Screen Readers)", "Form Validation and Constraints"]
            }
        },
        {
            "id": 14,
            "question": "What is the benefit of using the <pre>details</pre> and <pre>summary</pre> elements in this FAQ section?\n[CODE]<section aria-labelledby=\"faq-heading\">\n  <h2 id=\"faq-heading\">Frequently Asked Questions</h2>\n  \n  <details>\n    <summary>What is your return policy?</summary>\n    <p>We offer a 30-day return policy for all unused products...</p>\n  </details>\n  \n  <details>\n    <summary>Do you ship internationally?</summary>\n    <p>Yes, we ship to over 50 countries worldwide...</p>\n  </details>\n  \n  <details>\n    <summary>How can I track my order?</summary>\n    <p>You can track your order using the tracking number sent to your email...</p>\n  </details>\n</section>[/CODE]",
            "options": {
                "A": "They provide built-in expand/collapse functionality without JavaScript",
                "B": "They improve SEO for FAQ content",
                "C": "They automatically create a table of contents",
                "D": "They make content load faster"
            },
            "correct": "A",
            "explanation": "The <pre>details</pre> and <pre>summary</pre> elements provide built-in expand/collapse functionality without requiring JavaScript. They are semantically correct, accessible by default, and work across all modern browsers.\n\n[CODE]// Benefits of details/summary elements:\n\n// 1. No JavaScript required\n// 2. Built-in accessibility (screen readers announce state)\n// 3. Semantic structure\n// 4. Progressive enhancement friendly\n\n// Enhanced FAQ with additional features\n<section aria-labelledby=\"faq-heading\">\n  <h2 id=\"faq-heading\">Frequently Asked Questions</h2>\n  \n  <details open>\n    <summary>\n      <span class=\"question\">What is your return policy?</span>\n      <span class=\"icon\" aria-hidden=\"true\">▼</span>\n    </summary>\n    <div class=\"answer\">\n      <p>We offer a 30-day return policy for all unused products...</p>\n    </div>\n  </details>\n  \n  <details>\n    <summary>\n      <span class=\"question\">Do you ship internationally?</span>\n      <span class=\"icon\" aria-hidden=\"true\">▼</span>\n    </summary>\n    <div class=\"answer\">\n      <p>Yes, we ship to over 50 countries worldwide...</p>\n      <ul>\n        <li>North America: 3-5 business days</li>\n        <li>Europe: 5-7 business days</li>\n        <li>Asia: 7-10 business days</li>\n      </ul>\n    </div>\n  </details>\n</section>\n\n// CSS for custom styling\ndetails {\n  border: 1px solid #ddd;\n  border-radius: 4px;\n  margin-bottom: 8px;\n}\n\nsummary {\n  padding: 12px 16px;\n  cursor: pointer;\n  background: #f9f9f9;\n  font-weight: bold;\n}\n\nsummary:hover {\n  background: #f0f0f0;\n}\n\n.answer {\n  padding: 16px;\n  border-top: 1px solid #ddd;\n}\n\n// JavaScript enhancement for analytics\ndocument.querySelectorAll('details').forEach(details => {\n  details.addEventListener('toggle', function() {\n    if (this.open) {\n      // Track FAQ expansion for analytics\n      const question = this.querySelector('.question').textContent;\n      analytics.track('faq_expanded', { question });\n    }\n  });\n});\n\n// Alternative: Programmatic control\nfunction expandAllFAQs() {\n  document.querySelectorAll('details').forEach(details => {\n    details.open = true;\n  });\n}\n\nfunction collapseAllFAQs() {\n  document.querySelectorAll('details').forEach(details => {\n    details.open = false;\n  });\n}\n\n// Enhanced with search functionality\nfunction filterFAQs(searchTerm) {\n  document.querySelectorAll('details').forEach(details => {\n    const text = details.textContent.toLowerCase();\n    const shouldShow = text.includes(searchTerm.toLowerCase());\n    details.style.display = shouldShow ? 'block' : 'none';\n  });\n}\n\n// Fallback for older browsers\nif (!('open' in document.createElement('details'))) {\n  // Load polyfill or implement JavaScript fallback\n  import('details-polyfill');\n}\n\n// Using with structured data for SEO\n<script type=\"application/ld+json\">\n{\n  \"@context\": \"https://schema.org\",\n  \"@type\": \"FAQPage\",\n  \"mainEntity\": [\n    {\n      \"@type\": \"Question\",\n      \"name\": \"What is your return policy?\",\n      \"acceptedAnswer\": {\n        \"@type\": \"Answer\",\n        \"text\": \"We offer a 30-day return policy for all unused products...\"\n      }\n    }\n  ]\n}\n</script>[/CODE]\n\nThe <pre>details</pre> and <pre>summary</pre> elements provide a native, accessible solution for collapsible content that enhances user experience without JavaScript dependencies.",
            "topic": {
                "topics": ["Semantic HTML5 Elements", "Accessibility (ARIA, Screen Readers)"]
            }
        },
        {
            "id": 15,
            "question": "What performance optimization is achieved with this resource hint implementation?\n[CODE]<!-- Preconnect to critical third-party domains -->\n<link rel=\"preconnect\" href=\"https://fonts.googleapis.com\">\n<link rel=\"preconnect\" href=\"https://cdn.example.com\" crossorigin>\n\n<!-- Preload critical resources -->\n<link rel=\"preload\" href=\"hero-image.webp\" as=\"image\" type=\"image/webp\">\n<link rel=\"preload\" href=\"critical.css\" as=\"style\">\n<link rel=\"preload\" href=\"app.js\" as=\"script\">\n\n<!-- Prefetch likely navigation targets -->\n<link rel=\"prefetch\" href=\"/about\" as=\"document\">\n<link rel=\"prefetch\" href=\"/contact\" as=\"document\">\n\n<!-- DNS prefetch for additional domains -->\n<link rel=\"dns-prefetch\" href=\"//analytics.example.com\">\n<link rel=\"dns-prefetch\" href=\"//widgets.example.com\">[/CODE]",
            "options": {
                "A": "They reduce JavaScript execution time",
                "B": "They eliminate the need for caching",
                "C": "They reduce resource loading latency",
                "D": "They compress HTML files"
            },
            "correct": "C",
            "explanation": "Resource hints reduce loading latency by telling the browser about important resources and connections before they're actually needed, allowing the browser to prepare and optimize loading.\n\n[CODE]// Resource hints explained:\n\n// 1. preconnect - establishes early connection to origin\n<link rel=\"preconnect\" href=\"https://api.example.com\">\n// Use for: CDNs, APIs, fonts, critical third-party domains\n\n// 2. dns-prefetch - performs DNS lookup in advance\n<link rel=\"dns-prefetch\" href=\"//cdn.example.com\">\n// Use for: non-critical third-party domains\n\n// 3. preload - forces early fetch of resource\n<link rel=\"preload\" href=\"font.woff2\" as=\"font\" type=\"font/woff2\" crossorigin>\n// Use for: critical fonts, above-fold images, essential scripts\n\n// 4. prefetch - fetches resource for likely future navigation\n<link rel=\"prefetch\" href=\"/next-page.html\" as=\"document\">\n// Use for: likely next page visits, on-hover prefetching\n\n// 5. prerender - loads and renders entire page in background\n<link rel=\"prerender\" href=\"/checkout\">\n// Use sparingly: only for very likely next pages\n\n// Practical implementation patterns:\n\n// Critical above-the-fold resources\n<link rel=\"preload\" href=\"hero-image.avif\" as=\"image\" type=\"image/avif\">\n<link rel=\"preload\" href=\"critical-font.woff2\" as=\"font\" type=\"font/woff2\" crossorigin>\n\n// Third-party connections\n<link rel=\"preconnect\" href=\"https://fonts.gstatic.com\">\n<link rel=\"dns-prefetch\" href=\"https://www.google-analytics.com\">\n\n// Dynamic resource hints based on user behavior\nfunction prefetchOnHover(linkElement) {\n  linkElement.addEventListener('mouseenter', function() {\n    const href = this.href;\n    const link = document.createElement('link');\n    link.rel = 'prefetch';\n    link.href = href;\n    link.as = 'document';\n    document.head.appendChild(link);\n  }, { once: true });\n}\n\n// Usage\ndocument.querySelectorAll('a[data-prefetch]').forEach(prefetchOnHover);\n\n// Programmatic preloading\nfunction preloadResource(url, as) {\n  const link = document.createElement('link');\n  link.rel = 'preload';\n  link.href = url;\n  link.as = as;\n  document.head.appendChild(link);\n}\n\n// Load critical image when needed\nif (isAboveTheFold()) {\n  preloadResource('hero-image.jpg', 'image');\n}\n\n// Module preloading for JavaScript\n<link rel=\"modulepreload\" href=\"/src/main.js\">\n<link rel=\"modulepreload\" href=\"/src/utils.js\">\n\n// Responsive image preloading\n<link \n  rel=\"preload\" \n  href=\"hero-small.jpg\" \n  as=\"image\" \n  media=\"(max-width: 600px)\">\n<link \n  rel=\"preload\" \n  href=\"hero-large.jpg\" \n  as=\"image\" \n  media=\"(min-width: 601px)\">\n\n// Monitoring resource hint effectiveness\n// Use PerformanceObserver to track resource timing\nconst observer = new PerformanceObserver((list) => {\n  list.getEntries().forEach((entry) => {\n    if (entry.initiatorType === 'link' && entry.name.includes('preload')) {\n      console.log('Preloaded resource:', entry.name, entry.duration);\n    }\n  });\n});\n\nobserver.observe({entryTypes: ['resource']});[/CODE]\n\nResource hints are powerful performance optimization tools that can significantly reduce perceived load times when used strategically for critical resources and likely user actions.",
            "topic": {
                "topics": ["Performance Optimization", "Progressive Web App Features"]
            }
        },
        {
            "id": 16,
            "question": "What is the purpose of the <pre>main</pre> element with the <pre>role=\"main\"</pre> attribute in this layout?\n[CODE]<body>\n  <header role=\"banner\">\n    <h1>Website Title</h1>\n    <nav role=\"navigation\" aria-label=\"Main navigation\">\n      <!-- Navigation links -->\n    </nav>\n  </header>\n  \n  <main role=\"main\" id=\"main-content\">\n    <article>\n      <h2>Article Title</h2>\n      <p>Main content of the page...</p>\n    </article>\n  </main>\n  \n  <aside role=\"complementary\" aria-label=\"Related information\">\n    <section>\n      <h3>Related Articles</h3>\n      <!-- Sidebar content -->\n    </section>\n  </aside>\n  \n  <footer role=\"contentinfo\">\n    <p>&copy; 2024 Company Name</p>\n  </footer>\n</body>[/CODE]",
            "options": {
                "A": "It improves SEO ranking",
                "B": "It identifies the primary content of the page for accessibility",
                "C": "It makes the content load faster",
                "D": "It enables CSS grid layout"
            },
            "correct": "B",
            "explanation": "The <pre>main</pre> element with <pre>role=\"main\"</pre> identifies the primary content of the page for accessibility tools. This allows screen reader users to quickly navigate to the main content area, skipping repetitive navigation elements.\n\n[CODE]// Semantic HTML5 layout landmarks\n\n// 1. Header (role=\"banner\")\n<header role=\"banner\">\n  <!-- Site-wide header content -->\n</header>\n\n// 2. Navigation (role=\"navigation\")\n<nav role=\"navigation\" aria-label=\"Main navigation\">\n  <!-- Primary navigation -->\n</nav>\n\n<nav role=\"navigation\" aria-label=\"Secondary navigation\">\n  <!-- Secondary navigation -->\n</nav>\n\n// 3. Main content (role=\"main\")\n<main role=\"main\" id=\"main-content\">\n  <!-- Unique page content -->\n</main>\n\n// 4. Complementary content (role=\"complementary\")\n<aside role=\"complementary\" aria-label=\"Related articles\">\n  <!-- Sidebar content -->\n</aside>\n\n// 5. Footer (role=\"contentinfo\")\n<footer role=\"contentinfo\">\n  <!-- Site-wide footer content -->\n</footer>\n\n// Additional landmarks for complex applications\n\n// Search landmark\n<form role=\"search\" aria-label=\"Site search\">\n  <!-- Search form -->\n</form>\n\n// Form landmark\n<form role=\"form\" aria-labelledby=\"form-title\">\n  <h2 id=\"form-title\">Contact Form</h2>\n  <!-- Form fields -->\n</form>\n\n// Region landmark for custom widgets\n<div role=\"region\" aria-labelledby=\"widget-title\">\n  <h3 id=\"widget-title\">Weather Widget</h3>\n  <!-- Widget content -->\n</div>\n\n// Skip link for keyboard navigation\n<a href=\"#main-content\" class=\"skip-link\">\n  Skip to main content\n</a>\n\n// CSS for skip link\n.skip-link {\n  position: absolute;\n  top: -40px;\n  left: 6px;\n  background: #000;\n  color: #fff;\n  padding: 8px;\n  text-decoration: none;\n  z-index: 1000;\n}\n\n.skip-link:focus {\n  top: 6px;\n}\n\n// JavaScript for enhanced navigation\n// Allow screen readers to announce page regions\ndocument.addEventListener('DOMContentLoaded', function() {\n  const main = document.querySelector('main[role=\"main\"]');\n  if (main) {\n    main.setAttribute('tabindex', '-1');\n    \n    // Focus main content when skip link is used\n    const skipLink = document.querySelector('.skip-link');\n    if (skipLink) {\n      skipLink.addEventListener('click', function() {\n        main.focus();\n      });\n    }\n  }\n});\n\n// Multiple main elements in single-page applications\n// Only one main element should be visible at a time\nfunction showView(viewId) {\n  // Hide all main elements\n  document.querySelectorAll('main').forEach(main => {\n    main.hidden = true;\n    main.removeAttribute('role');\n  });\n  \n  // Show target main element\n  const targetMain = document.getElementById(viewId);\n  if (targetMain) {\n    targetMain.hidden = false;\n    targetMain.setAttribute('role', 'main');\n    targetMain.focus();\n  }\n}\n\n// Structured data for better SEO\n<script type=\"application/ld+json\">\n{\n  \"@context\": \"https://schema.org\",\n  \"@type\": \"WebPage\",\n  \"mainEntity\": {\n    \"@type\": \"Article\",\n    \"headline\": \"Article Title\",\n    \"description\": \"Article description...\"\n  }\n}\n</script>[/CODE]\n\nUsing proper landmark roles and semantic elements creates a more accessible and navigable experience for all users, particularly those using assistive technologies.",
            "topic": {
                "topics": ["Semantic HTML5 Elements", "Accessibility (ARIA, Screen Readers)"]
            }
        },
        {
            "id": 17,
            "question": "What is the security vulnerability in this inline event handler?\n[CODE]<button onclick=\"deleteUser('{{userId}}')\">Delete User</button>\n\n<script>\nfunction deleteUser(userId) {\n  if (confirm('Are you sure you want to delete user ' + userId + '?')) {\n    fetch('/api/users/' + userId, { method: 'DELETE' })\n      .then(response => response.json())\n      .then(data => {\n        alert('User deleted: ' + data.message);\n      });\n  }\n}\n</script>[/CODE]",
            "options": {
                "A": "XSS vulnerability through user ID injection",
                "B": "CSRF vulnerability missing token",
                "C": "Missing input validation",
                "D": "All of the above"
            },
            "correct": "D",
            "explanation": "This code has multiple security vulnerabilities: XSS through user ID injection, CSRF vulnerability missing protection tokens, and missing input validation on both client and server sides.\n\n[CODE]// Secure implementation\n\n// HTML - use data attributes instead of inline handlers\n<button \n  class=\"delete-user\" \n  data-user-id=\"{{userId}}\"\n  data-csrf-token=\"{{csrfToken}}\">\n  Delete User\n</button>\n\n// JavaScript - event delegation with security measures\ndocument.addEventListener('click', async function(e) {\n  if (e.target.classList.contains('delete-user')) {\n    const button = e.target;\n    const userId = button.dataset.userId;\n    const csrfToken = button.dataset.csrfToken;\n    \n    // Input validation\n    if (!isValidUserId(userId)) {\n      showError('Invalid user ID');\n      return;\n    }\n    \n    if (!isValidCsrfToken(csrfToken)) {\n      showError('Invalid security token');\n      return;\n    }\n    \n    // Secure confirmation\n    const confirmed = await showConfirmationDialog(\n      `Are you sure you want to delete user ${escapeHTML(userId)}?`\n    );\n    \n    if (confirmed) {\n      await deleteUserSecure(userId, csrfToken);\n    }\n  }\n});\n\n// Secure delete function\nasync function deleteUserSecure(userId, csrfToken) {\n  try {\n    const response = await fetch(`/api/users/${encodeURIComponent(userId)}`, {\n      method: 'DELETE',\n      headers: {\n        'Content-Type': 'application/json',\n        'X-CSRF-Token': csrfToken,\n        'X-Requested-With': 'XMLHttpRequest'\n      },\n      credentials: 'same-origin'\n    });\n    \n    if (!response.ok) {\n      throw new Error(`HTTP ${response.status}`);\n    }\n    \n    const data = await response.json();\n    \n    // Sanitize response before displaying\n    showSuccess(`User deleted: ${escapeHTML(data.message)}`);\n    \n  } catch (error) {\n    showError(`Delete failed: ${escapeHTML(error.message)}`);\n  }\n}\n\n// Security utility functions\nfunction escapeHTML(str) {\n  const div = document.createElement('div');\n  div.textContent = str;\n  return div.innerHTML;\n}\n\nfunction isValidUserId(userId) {\n  // Validate user ID format\n  return /^[a-zA-Z0-9-_]+$/.test(userId);\n}\n\nfunction isValidCsrfToken(token) {\n  // Validate CSRF token format\n  return typeof token === 'string' && token.length > 0;\n}\n\n// Secure confirmation dialog\nasync function showConfirmationDialog(message) {\n  return new Promise((resolve) => {\n    // Use a secure modal implementation\n    const modal = createSecureModal(message);\n    modal.show().then(resolve);\n  });\n}\n\n// Server-side security measures (Node.js example)\napp.delete('/api/users/:userId', (req, res) => {\n  // Validate CSRF token\n  if (!validateCsrfToken(req.headers['x-csrf-token'])) {\n    return res.status(403).json({ error: 'Invalid CSRF token' });\n  }\n  \n  // Validate user ID\n  const userId = req.params.userId;\n  if (!/^[a-zA-Z0-9-_]+$/.test(userId)) {\n    return res.status(400).json({ error: 'Invalid user ID' });\n  }\n  \n  // Check authorization\n  if (!hasPermission(req.user, 'delete_user', userId)) {\n    return res.status(403).json({ error: 'Insufficient permissions' });\n  }\n  \n  // Perform deletion\n  deleteUserFromDatabase(userId)\n    .then(() => {\n      res.json({ message: 'User deleted successfully' });\n    })\n    .catch(error => {\n      res.status(500).json({ error: 'Deletion failed' });\n    });\n});\n\n// Additional security headers\n// In your web server configuration or middleware\napp.use(helmet({\n  contentSecurityPolicy: {\n    directives: {\n      defaultSrc: [\"'self'\"],\n      scriptSrc: [\"'self'\", \"'unsafe-inline'\"],\n      styleSrc: [\"'self'\", \"'unsafe-inline'\"],\n    },\n  },\n  hsts: {\n    maxAge: 31536000,\n    includeSubDomains: true,\n    preload: true\n  }\n}));[/CODE]\n\nSecurity vulnerabilities in HTML and JavaScript can lead to serious breaches. Always validate inputs, use proper CSRF protection, and avoid inline event handlers with dynamic content.",
            "topic": {
                "topics": ["HTML5 APIs (Canvas, Geolocation, Storage)", "Form Validation and Constraints"]
            }
        },
        {
            "id": 18,
            "question": "What is the purpose of the <pre>manifest</pre> attribute in this PWA setup?\n[CODE]<!DOCTYPE html>\n<html lang=\"en\" manifest=\"app.webmanifest\">\n<head>\n  <meta charset=\"UTF-8\">\n  <meta name=\"viewport\" content=\"width=device-width, initial-scale=1.0\">\n  <title>Progressive Web App</title>\n  \n  <!-- Web App Manifest -->\n  <link rel=\"manifest\" href=\"app.webmanifest\">\n  \n  <!-- Theme color for browser UI -->\n  <meta name=\"theme-color\" content=\"#2F855A\">\n  \n  <!-- iOS Safari meta tags -->\n  <meta name=\"apple-mobile-web-app-capable\" content=\"yes\">\n  <meta name=\"apple-mobile-web-app-status-bar-style\" content=\"default\">\n  <meta name=\"apple-mobile-web-app-title\" content=\"My PWA\">\n  \n  <!-- Icons for various platforms -->\n  <link rel=\"icon\" href=\"/icons/icon-192.png\" type=\"image/png\">\n  <link rel=\"apple-touch-icon\" href=\"/icons/icon-180.png\">\n</head>\n<body>\n  <main>\n    <h1>Welcome to My PWA</h1>\n    <p>This is a progressive web application.</p>\n  </main>\n  \n  <script>\n    // Service Worker registration\n    if ('serviceWorker' in navigator) {\n      navigator.serviceWorker.register('/sw.js');\n    }\n  </script>\n</body>\n</html>[/CODE]",
            "options": {
                "A": "It enables offline functionality",
                "B": "It defines app metadata for installation and appearance",
                "C": "It improves page loading speed",
                "D": "It enables push notifications"
            },
            "correct": "B",
            "explanation": "The <pre>manifest</pre> attribute and associated web app manifest file define the app's metadata for installation and appearance when added to home screens. It controls how the app looks and behaves when installed as a PWA.\n\n[CODE]// app.webmanifest content\n{\n  \"name\": \"My Progressive Web App\",\n  \"short_name\": \"MyPWA\",\n  \"description\": \"A sample progressive web application\",\n  \"start_url\": \"/\",\n  \"display\": \"standalone\",\n  \"background_color\": \"#ffffff\",\n  \"theme_color\": \"#2F855A\",\n  \"orientation\": \"portrait-primary\",\n  \"scope\": \"/\",\n  \"lang\": \"en-US\",\n  \"categories\": [\"productivity\", \"utilities\"],\n  \"icons\": [\n    {\n      \"src\": \"/icons/icon-72.png\",\n      \"sizes\": \"72x72\",\n      \"type\": \"image/png\",\n      \"purpose\": \"any\"\n    },\n    {\n      \"src\": \"/icons/icon-96.png\",\n      \"sizes\": \"96x96\",\n      \"type\": \"image/png\",\n      \"purpose\": \"any\"\n    },\n    {\n      \"src\": \"/icons/icon-128.png\",\n      \"sizes\": \"128x128\",\n      \"type\": \"image/png\",\n      \"purpose\": \"any\"\n    },\n    {\n      \"src\": \"/icons/icon-144.png\",\n      \"sizes\": \"144x144\",\n      \"type\": \"image/png\",\n      \"purpose\": \"any\"\n    },\n    {\n      \"src\": \"/icons/icon-152.png\",\n      \"sizes\": \"152x152\",\n      \"type\": \"image/png\",\n      \"purpose\": \"any\"\n    },\n    {\n      \"src\": \"/icons/icon-192.png\",\n      \"sizes\": \"192x192\",\n      \"type\": \"image/png\",\n      \"purpose\": \"any maskable\"\n    },\n    {\n      \"src\": \"/icons/icon-384.png\",\n      \"sizes\": \"384x384\",\n      \"type\": \"image/png\",\n      \"purpose\": \"any\"\n    },\n    {\n      \"src\": \"/icons/icon-512.png\",\n      \"sizes\": \"512x512\",\n      \"type\": \"image/png\",\n      \"purpose\": \"any maskable\"\n    }\n  ],\n  \"screenshots\": [\n    {\n      \"src\": \"/screenshots/desktop.png\",\n      \"sizes\": \"1280x720\",\n      \"type\": \"image/png\",\n      \"form_factor\": \"wide\"\n    },\n    {\n      \"src\": \"/screenshots/mobile.png\",\n      \"sizes\": \"375x667\",\n      \"type\": \"image/png\",\n      \"form_factor\": \"narrow\"\n    }\n  ],\n  \"shortcuts\": [\n    {\n      \"name\": \"New Item\",\n      \"short_name\": \"New\",\n      \"description\": \"Create a new item\",\n      \"url\": \"/new\",\n      \"icons\": [\n        {\n          \"src\": \"/icons/shortcut-96.png\",\n          \"sizes\": \"96x96\",\n          \"type\": \"image/png\"\n        }\n      ]\n    }\n  ]\n}\n\n// Enhanced HTML with additional PWA features\n\n// Additional meta tags for better PWA support\n<meta name=\"application-name\" content=\"My PWA\">\n<meta name=\"mobile-web-app-capable\" content=\"yes\">\n\n// Splash screen for iOS\n<link \n  rel=\"apple-touch-startup-image\" \n  href=\"/splash/apple-splash-2048-2732.jpg\"\n  media=\"(device-width: 1024px) and (device-height: 1366px) and (-webkit-device-pixel-ratio: 2) and (orientation: portrait)\">\n\n// Theme color variations\n<meta name=\"theme-color\" content=\"#2F855A\" media=\"(prefers-color-scheme: light)\">\n<meta name=\"theme-color\" content=\"#1A202C\" media=\"(prefers-color-scheme: dark)\">\n\n// Display modes explained:\n// - fullscreen: No browser UI\n// - standalone: App-like experience\n// - minimal-ui: Minimal browser controls\n// - browser: Regular browser experience\n\n// JavaScript for installation prompt\nlet deferredPrompt;\n\nwindow.addEventListener('beforeinstallprompt', (e) => {\n  // Prevent Chrome 67 and earlier from automatically showing the prompt\n  e.preventDefault();\n  // Stash the event so it can be triggered later\n  deferredPrompt = e;\n  // Update UI to notify the user they can add to home screen\n  showInstallPromotion();\n});\n\n// Installation function\nasync function installPWA() {\n  if (deferredPrompt) {\n    deferredPrompt.prompt();\n    const { outcome } = await deferredPrompt.userChoice;\n    \n    if (outcome === 'accepted') {\n      console.log('User accepted the install prompt');\n    } else {\n      console.log('User dismissed the install prompt');\n    }\n    \n    deferredPrompt = null;\n  }\n}\n\n// Check if app is installed\nwindow.addEventListener('appinstalled', (evt) => {\n  console.log('PWA was installed');\n  // Track installation in analytics\n  gtag('event', 'pwa_installed');\n});\n\n// Detect display mode\nfunction getDisplayMode() {\n  const isStandalone = window.matchMedia('(display-mode: standalone)').matches;\n  if (document.referrer.startsWith('android-app://')) {\n    return 'twa';\n  } else if (navigator.standalone || isStandalone) {\n    return 'standalone';\n  }\n  return 'browser';\n}[/CODE]\n\nThe web app manifest is a crucial component of PWAs that enables installation, controls appearance, and provides a native app-like experience to users.",
            "topic": {
                "topics": ["Progressive Web App Features", "Performance Optimization"]
            }
        },
        {
            "id": 19,
            "question": "What is the benefit of using the <pre>dialog</pre> element in this modal implementation?\n[CODE]<!-- Dialog trigger -->\n<button onclick=\"settingsDialog.showModal()\">Open Settings</button>\n\n<!-- Dialog element -->\n<dialog id=\"settingsDialog\" aria-labelledby=\"settingsTitle\">\n  <form method=\"dialog\">\n    <h2 id=\"settingsTitle\">Application Settings</h2>\n    \n    <div>\n      <label for=\"theme\">Theme:</label>\n      <select id=\"theme\" name=\"theme\">\n        <option value=\"light\">Light</option>\n        <option value=\"dark\">Dark</option>\n        <option value=\"auto\">Auto</option>\n      </select>\n    </div>\n    \n    <div>\n      <label for=\"notifications\">\n        <input type=\"checkbox\" id=\"notifications\" name=\"notifications\">\n        Enable notifications\n      </label>\n    </div>\n    \n    <div>\n      <button type=\"submit\" value=\"save\">Save</button>\n      <button type=\"submit\" value=\"cancel\" formnovalidate>Cancel</button>\n    </div>\n  </form>\n</dialog>\n\n<script>\nconst dialog = document.getElementById('settingsDialog');\n\n// Handle dialog close\ndialog.addEventListener('close', () => {\n  if (dialog.returnValue === 'save') {\n    const formData = new FormData(dialog.querySelector('form'));\n    saveSettings(formData);\n  }\n});\n\n// Close on backdrop click\ndialog.addEventListener('click', (event) => {\n  if (event.target === dialog) {\n    dialog.close('cancel');\n  }\n});\n</script>[/CODE]",
            "options": {
                "A": "It provides built-in accessibility and focus management",
                "B": "It automatically saves form data",
                "C": "It works without JavaScript",
                "D": "It improves SEO"
            },
            "correct": "A",
            "explanation": "The <pre>dialog</pre> element provides built-in accessibility features and focus management. It automatically traps focus within the dialog, manages screen reader announcements, and provides proper semantic structure for modal content.\n\n[CODE]// Benefits of the dialog element:\n\n// 1. Built-in focus trapping\n// 2. Automatic backdrop layer\n// 3. Native ESC key handling\n// 4. Proper ARIA roles and attributes\n// 5. Top layer positioning (above all other content)\n\n// Enhanced dialog implementation\n<dialog \n  id=\"enhancedDialog\" \n  aria-labelledby=\"dialogTitle\"\n  aria-describedby=\"dialogDesc\">\n  \n  <form method=\"dialog\">\n    <div class=\"dialog-header\">\n      <h2 id=\"dialogTitle\">Enhanced Dialog</h2>\n      <button \n        type=\"submit\" \n        value=\"close\" \n        aria-label=\"Close dialog\"\n        class=\"close-button\">\n        ×\n      </button>\n    </div>\n    \n    <div class=\"dialog-body\">\n      <p id=\"dialogDesc\">This is an enhanced dialog with better accessibility.</p>\n      \n      <div class=\"form-group\">\n        <label for=\"username\">Username</label>\n        <input \n          type=\"text\" \n          id=\"username\" \n          name=\"username\"\n          required\n          aria-required=\"true\">\n      </div>\n    </div>\n    \n    <div class=\"dialog-footer\">\n      <button type=\"submit\" value=\"confirm\">Confirm</button>\n      <button type=\"submit\" value=\"cancel\" formnovalidate>Cancel</button>\n    </div>\n  </form>\n</dialog>\n\n// JavaScript for enhanced dialog functionality\nclass DialogManager {\n  constructor(dialogId) {\n    this.dialog = document.getElementById(dialogId);\n    this.previouslyFocused = null;\n    \n    this.init();\n  }\n  \n  init() {\n    // Store focus when dialog opens\n    this.dialog.addEventListener('click', (e) => {\n      if (e.target === this.dialog) {\n        this.close();\n      }\n    });\n    \n    // Handle ESC key\n    this.dialog.addEventListener('keydown', (e) => {\n      if (e.key === 'Escape') {\n        e.preventDefault();\n        this.close();\n      }\n      \n      // Trap focus within dialog\n      if (e.key === 'Tab') {\n        this.trapFocus(e);\n      }\n    });\n    \n    // Announce dialog to screen readers\n    this.dialog.addEventListener('close', () => {\n      this.restoreFocus();\n    });\n  }\n  \n  open() {\n    this.previouslyFocused = document.activeElement;\n    this.dialog.showModal();\n    \n    // Focus first focusable element\n    const firstFocusable = this.dialog.querySelector(\n      'button, [href], input, select, textarea, [tabindex]:not([tabindex=\"-1\"])'\n    );\n    \n    if (firstFocusable) {\n      firstFocusable.focus();\n    }\n  }\n  \n  close(returnValue = 'cancel') {\n    this.dialog.close(returnValue);\n  }\n  \n  trapFocus(e) {\n    const focusableElements = this.dialog.querySelectorAll(\n      'button, [href], input, select, textarea, [tabindex]:not([tabindex=\"-1\"])'\n    );\n    \n    const firstElement = focusableElements[0];\n    const lastElement = focusableElements[focusableElements.length - 1];\n    \n    if (e.shiftKey && document.activeElement === firstElement) {\n      lastElement.focus();\n      e.preventDefault();\n    } else if (!e.shiftKey && document.activeElement === lastElement) {\n      firstElement.focus();\n      e.preventDefault();\n    }\n  }\n  \n  restoreFocus() {\n    if (this.previouslyFocused && this.previouslyFocused.focus) {\n      this.previouslyFocused.focus();\n    }\n  }\n}\n\n// Usage\nconst settingsDialog = new DialogManager('enhancedDialog');\n\n// Open dialog\ndocument.getElementById('openDialog').addEventListener('click', () => {\n  settingsDialog.open();\n});\n\n// Handle dialog results\nconst dialog = document.getElementById('enhancedDialog');\ndialog.addEventListener('close', () => {\n  switch(dialog.returnValue) {\n    case 'confirm':\n      handleConfirm();\n      break;\n    case 'cancel':\n      handleCancel();\n      break;\n  }\n});\n\n// CSS for dialog styling\ndialog::backdrop {\n  background: rgba(0, 0, 0, 0.5);\n  backdrop-filter: blur(2px);\n}\n\n.dialog-header {\n  display: flex;\n  justify-content: space-between;\n  align-items: center;\n  padding-bottom: 16px;\n  border-bottom: 1px solid #e2e8f0;\n}\n\n.close-button {\n  background: none;\n  border: none;\n  font-size: 24px;\n  cursor: pointer;\n  padding: 4px 8px;\n}\n\n.close-button:hover {\n  background: #f7fafc;\n  border-radius: 4px;\n}\n\n// Fallback for older browsers\nif (!window.HTMLDialogElement) {\n  // Load dialog polyfill\n  import('https://cdn.jsdelivr.net/npm/dialog-polyfill@0.5.6/dist/dialog-polyfill.esm.js')\n    .then(module => {\n      const dialogPolyfill = module.default;\n      dialogPolyfill.registerDialog(document.getElementById('enhancedDialog'));\n    });\n}[/CODE]\n\nThe <pre>dialog</pre> element provides a native, accessible solution for modal dialogs that simplifies implementation and ensures proper accessibility out of the box.",
            "topic": {
                "topics": ["Semantic HTML5 Elements", "Accessibility (ARIA, Screen Readers)"]
            }
        },
        {
            "id": 20,
            "question": "What optimization technique is demonstrated in this <pre>iframe</pre> implementation?\n[CODE]<!-- Lazy-loaded iframe for third-party content -->\n<div class=\"iframe-container\">\n  <iframe \n    data-src=\"https://www.youtube.com/embed/VIDEO_ID\"\n    title=\"Product demonstration video\"\n    width=\"560\" \n    height=\"315\"\n    loading=\"lazy\"\n    allow=\"accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture\"\n    allowfullscreen>\n  </iframe>\n  \n  <!-- Loading placeholder -->\n  <div class=\"iframe-placeholder\">\n    <div class=\"loading-spinner\"></div>\n    <p>Loading video...</p>\n  </div>\n</div>\n\n<script>\n// Lazy load iframes when they enter viewport\nfunction lazyLoadIframes() {\n  const iframes = document.querySelectorAll('iframe[data-src]');\n  \n  const observer = new IntersectionObserver((entries) => {\n    entries.forEach(entry => {\n      if (entry.isIntersecting) {\n        const iframe = entry.target;\n        iframe.src = iframe.dataset.src;\n        iframe.removeAttribute('data-src');\n        \n        // Hide placeholder when loaded\n        iframe.addEventListener('load', () => {\n          iframe.parentElement.querySelector('.iframe-placeholder').style.display = 'none';\n        });\n        \n        observer.unobserve(iframe);\n      }\n    });\n  });\n  \n  iframes.forEach(iframe => observer.observe(iframe));\n}\n\n// Initialize on DOM ready\ndocument.addEventListener('DOMContentLoaded', lazyLoadIframes);\n</script>[/CODE]",
            "options": {
                "A": "It reduces initial page load time by deferring iframe loading",
                "B": "It improves iframe security",
                "C": "It enables iframe responsive design",
                "D": "It automatically compresses iframe content"
            },
            "correct": "A",
            "explanation": "This implementation reduces initial page load time by deferring iframe loading until they enter the viewport. Third-party iframes (like YouTube videos) can significantly impact page performance, so lazy loading them improves Core Web Vitals.\n\n[CODE]// Advanced iframe optimization techniques\n\n// 1. Lazy loading with Intersection Observer\nclass LazyIframeLoader {\n  constructor() {\n    this.observer = null;\n    this.init();\n  }\n  \n  init() {\n    this.observer = new IntersectionObserver(this.handleIntersection.bind(this), {\n      rootMargin: '100px', // Start loading 100px before viewport\n      threshold: 0.1\n    });\n    \n    this.observeIframes();\n  }\n  \n  observeIframes() {\n    const iframes = document.querySelectorAll('iframe[data-src]');\n    iframes.forEach(iframe => this.observer.observe(iframe));\n  }\n  \n  handleIntersection(entries) {\n    entries.forEach(entry => {\n      if (entry.isIntersecting) {\n        const iframe = entry.target;\n        this.loadIframe(iframe);\n        this.observer.unobserve(iframe);\n      }\n    });\n  }\n  \n  loadIframe(iframe) {\n    // Show loading state\n    const placeholder = iframe.parentElement.querySelector('.iframe-placeholder');\n    if (placeholder) {\n      placeholder.classList.add('loading');\n    }\n    \n    // Set src to trigger loading\n    iframe.src = iframe.dataset.src;\n    iframe.removeAttribute('data-src');\n    \n    // Handle load completion\n    iframe.addEventListener('load', () => {\n      if (placeholder) {\n        placeholder.style.display = 'none';\n      }\n      this.trackIframeLoad(iframe);\n    });\n    \n    // Handle errors\n    iframe.addEventListener('error', () => {\n      if (placeholder) {\n        placeholder.innerHTML = '<p>Failed to load content</p>';\n        placeholder.classList.add('error');\n      }\n    });\n  }\n  \n  trackIframeLoad(iframe) {\n    // Track performance metrics\n    const loadTime = performance.now();\n    console.log(`Iframe loaded in ${loadTime}ms`);\n    \n    // Send to analytics\n    if (window.gtag) {\n      gtag('event', 'iframe_loaded', {\n        'event_category': 'Performance',\n        'event_label': iframe.title || 'Unknown',\n        'value': Math.round(loadTime)\n      });\n    }\n  }\n}\n\n// 2. Responsive iframe container\n.iframe-container {\n  position: relative;\n  width: 100%;\n  height: 0;\n  padding-bottom: 56.25%; /* 16:9 aspect ratio */\n  overflow: hidden;\n}\n\n.iframe-container iframe {\n  position: absolute;\n  top: 0;\n  left: 0;\n  width: 100%;\n  height: 100%;\n  border: 0;\n}\n\n.iframe-placeholder {\n  position: absolute;\n  top: 0;\n  left: 0;\n  width: 100%;\n  height: 100%;\n  display: flex;\n  flex-direction: column;\n  align-items: center;\n  justify-content: center;\n  background: #f7fafc;\n  border: 1px solid #e2e8f0;\n}\n\n.loading-spinner {\n  width: 40px;\n  height: 40px;\n  border: 4px solid #e2e8f0;\n  border-top: 4px solid #4299e1;\n  border-radius: 50%;\n  animation: spin 1s linear infinite;\n}\n\n@keyframes spin {\n  0% { transform: rotate(0deg); }\n  100% { transform: rotate(360deg); }\n}\n\n// 3. Enhanced iframe with performance monitoring\n<iframe \n  data-src=\"https://example.com/embed\"\n  title=\"Third-party content\"\n  loading=\"lazy\"\n  referrerpolicy=\"no-referrer-when-downgrade\"\n  sandbox=\"allow-scripts allow-same-origin allow-popups\"\n  onload=\"this.dataset.loaded = true;\">\n</iframe>\n\n// 4. Iframe communication with postMessage\n// Parent window\nconst iframe = document.getElementById('myIframe');\niframe.addEventListener('load', () => {\n  iframe.contentWindow.postMessage({\n    type: 'INIT',\n    config: { theme: 'dark' }\n  }, 'https://embedded-site.com');\n});\n\n// Listen for messages from iframe\nwindow.addEventListener('message', (event) => {\n  if (event.origin !== 'https://embedded-site.com') return;\n  \n  if (event.data.type === 'READY') {\n    console.log('Iframe is ready');\n  }\n});\n\n// 5. Dynamic iframe src based on conditions\nfunction loadOptimizedIframe(container, url) {\n  const iframe = document.createElement('iframe');\n  \n  // Only load if user interacts or conditions are met\n  if (isConnectionGood() && isUserInterested()) {\n    iframe.src = url;\n  } else {\n    iframe.dataset.src = url;\n    iframe.loading = 'lazy';\n  }\n  \n  container.appendChild(iframe);\n}\n\n// Connection quality check\nfunction isConnectionGood() {\n  return navigator.connection \n    ? navigator.connection.effectiveType !== 'slow-2g'\n    : true;\n}\n\n// User interest detection\nfunction isUserInterested() {\n  // Check if user has scrolled near the iframe\n  // or shown other engagement signals\n  return true; // Implement based on your metrics\n}[/CODE]\n\nLazy loading iframes significantly improves page performance, especially when dealing with multiple third-party embeds or heavy content that may not be immediately visible to users.",
            "topic": {
                "topics": ["Performance Optimization", "Media Elements and APIs"]
            }
        },
        {
            "id": 21,
            "question": "What is the purpose of the <pre>slot</pre> element in this web component composition?\n[CODE]<!-- Definition of user-card component -->\n<template id=\"user-card-template\">\n  <div class=\"user-card\">\n    <div class=\"card-header\">\n      <slot name=\"avatar\">\n        <img src=\"/default-avatar.png\" alt=\"Default avatar\">\n      </slot>\n      <slot name=\"name\">Unknown User</slot>\n    </div>\n    \n    <div class=\"card-body\">\n      <slot name=\"bio\">No biography provided</slot>\n    </div>\n    \n    <div class=\"card-footer\">\n      <slot name=\"actions\"></slot>\n    </div>\n  </div>\n</template>\n\n<!-- Usage of user-card with slotted content -->\n<user-card>\n  <img slot=\"avatar\" src=\"user-123.jpg\" alt=\"John Doe\">\n  <h2 slot=\"name\">John Doe</h2>\n  <p slot=\"bio\">Software developer with 5 years of experience...</p>\n  \n  <div slot=\"actions\">\n    <button>Follow</button>\n    <button>Message</button>\n  </div>\n</user-card>[/CODE]",
            "options": {
                "A": "It enables content projection from light DOM to shadow DOM",
                "B": "It improves component performance",
                "C": "It automatically styles slotted content",
                "D": "It enables component inheritance"
            },
            "correct": "A",
            "explanation": "The <pre>slot</pre> element enables content projection from the light DOM (user-provided content) to the shadow DOM (component's internal structure). This allows users of the component to customize its content while maintaining the component's structure and styling.\n\n[CODE]// Web component implementation with slots\nclass UserCard extends HTMLElement {\n  constructor() {\n    super();\n    const template = document.getElementById('user-card-template');\n    const content = template.content.cloneNode(true);\n    \n    this.attachShadow({ mode: 'open' }).appendChild(content);\n  }\n  \n  connectedCallback() {\n    // Set default attributes\n    if (!this.hasAttribute('role')) {\n      this.setAttribute('role', 'article');\n    }\n    \n    // Enhance accessibility\n    this.shadowRoot.querySelector('.user-card')\n      .setAttribute('aria-labelledby', 'user-name');\n  }\n}\n\ncustomElements.define('user-card', UserCard);\n\n// Advanced slot usage patterns\n\n// 1. Default slot content\n<template id=\"advanced-card\">\n  <div class=\"card\">\n    <!-- Named slot with fallback -->\n    <slot name=\"header\">\n      <h2>Default Header</h2>\n    </slot>\n    \n    <!-- Default slot (unnamed) -->\n    <slot>\n      <p>Default content</p>\n    </slot>\n    \n    <!-- Conditional slot -->\n    <slot name=\"footer\"></slot>\n  </div>\n</template>\n\n// 2. Slot change events\nclass SlotAwareComponent extends HTMLElement {\n  constructor() {\n    super();\n    this.attachShadow({ mode: 'open' });\n    this.shadowRoot.innerHTML = `\n      <div class=\"container\">\n        <slot name=\"content\"></slot>\n      </div>\n    `;\n  }\n  \n  connectedCallback() {\n    const slot = this.shadowRoot.querySelector('slot[name=\"content\"]');\n    \n    slot.addEventListener('slotchange', () => {\n      const nodes = slot.assignedNodes();\n      console.log('Slot content changed:', nodes);\n      \n      // Update component state based on slotted content\n      this.handleSlotChange(nodes);\n    });\n  }\n  \n  handleSlotChange(nodes) {\n    const hasContent = nodes.some(node => \n      node.nodeType === Node.ELEMENT_NODE && node.innerHTML.trim()\n    );\n    \n    this.toggleAttribute('empty', !hasContent);\n  }\n}\n\n// 3. Multiple slots with different content types\n<template id=\"media-card\">\n  <article class=\"media-card\">\n    <div class=\"media\">\n      <slot name=\"media\">\n        <!-- Can accept img, video, or iframe -->\n      </slot>\n    </div>\n    \n    <div class=\"content\">\n      <slot name=\"title\"></slot>\n      <slot name=\"description\"></slot>\n      <slot name=\"metadata\"></slot>\n    </div>\n    \n    <div class=\"actions\">\n      <slot name=\"actions\"></slot>\n    </div>\n  </article>\n</template>\n\n// Usage with different media types\n<media-card>\n  <img slot=\"media\" src=\"image.jpg\" alt=\"Description\">\n  <h3 slot=\"title\">Card Title</h3>\n  <p slot=\"description\">Card description...</p>\n  <div slot=\"actions\">\n    <button>Like</button>\n    <button>Share</button>\n  </div>\n</media-card>\n\n<media-card>\n  <video slot=\"media\" controls>\n    <source src=\"video.mp4\" type=\"video/mp4\">\n  </video>\n  <!-- Same slot structure for video -->\n</media-card>\n\n// 4. Slot styling with ::slotted pseudo-element\n// In shadow DOM styles\n<style>\n::slotted(img) {\n  max-width: 100%;\n  height: auto;\n  border-radius: 8px;\n}\n\n::slotted(h2) {\n  color: #2d3748;\n  margin: 0 0 8px 0;\n}\n\n::slotted(.highlight) {\n  background-color: yellow;\n  padding: 2px 4px;\n}\n\n/* Only style direct children */\n::slotted(> p) {\n  line-height: 1.6;\n}\n</style>\n\n// 5. Programmatic slot manipulation\nfunction getSlottedContent(component, slotName) {\n  const slot = component.shadowRoot.querySelector(`slot[name=\"${slotName}\"]`);\n  return slot.assignedNodes();\n}\n\nfunction hasSlotContent(component, slotName) {\n  const nodes = getSlottedContent(component, slotName);\n  return nodes.length > 0;\n}\n\nfunction setFallbackContent(component, slotName, content) {\n  const slot = component.shadowRoot.querySelector(`slot[name=\"${slotName}\"]`);\n  if (!hasSlotContent(component, slotName)) {\n    slot.innerHTML = content;\n  }\n}\n\n// Usage example\nconst card = document.querySelector('user-card');\nif (!hasSlotContent(card, 'actions')) {\n  setFallbackContent(card, 'actions', '<button>Default Action</button>');\n}[/CODE]\n\nSlots are a powerful feature of web components that enable flexible content composition while maintaining encapsulation and reusability. They bridge the gap between component consumers and component internals.",
            "topic": {
                "topics": ["Web Components and Custom Elements", "Shadow DOM and Encapsulation"]
            }
        },
        {
            "id": 22,
            "question": "What is the purpose of the <pre>cite</pre> and <pre>blockquote</pre> elements in this content structure?\n[CODE]<article>\n  <h1>The Future of Web Development</h1>\n  \n  <p>As we look towards the future of web technologies, one prominent figure noted:</p>\n  \n  <blockquote cite=\"https://example.com/interview\">\n    <p>The convergence of AI and web development will fundamentally change how we build user interfaces in the coming decade.</p>\n    \n    <footer>\n      — Dr. Jane Smith, \n      <cite>\n        <a href=\"https://example.com/interview\">\n          \"Web Technologies 2030\" Interview\n        </a>\n      </cite>\n    </footer>\n  </blockquote>\n  \n  <p>This perspective aligns with current trends in machine learning integration...</p>\n</article>[/CODE]",
            "options": {
                "A": "They improve SEO for quoted content",
                "B": "They provide semantic meaning for quotations and citations",
                "C": "They automatically style block quotations",
                "D": "They enable social media sharing of quotes"
            },
            "correct": "B",
            "explanation": "The <pre>blockquote</pre> and <pre>cite</pre> elements provide semantic meaning for quotations and their citations. They help browsers, search engines, and assistive technologies understand the structure and origin of quoted content.\n\n[CODE]// Semantic quotation patterns\n\n// 1. Basic blockquote with citation\n<blockquote cite=\"https://www.example.com/source\">\n  <p>This is a quoted paragraph of text that spans multiple lines and provides important context for the discussion.</p>\n  \n  <footer>\n    — Author Name, \n    <cite>\n      <a href=\"https://www.example.com/source\">Source Title</a>\n    </cite>\n  </footer>\n</blockquote>\n\n// 2. Inline quotation\n<p>\n  According to <cite>Web Standards Guide</cite>, \n  <q cite=\"https://example.com/standards\">semantic HTML improves accessibility</q>.\n</p>\n\n// 3. Nested quotations\n<blockquote>\n  <p>As the famous developer once said:</p>\n  \n  <blockquote cite=\"https://example.com/nested\">\n    <p>Always code as if the person who ends up maintaining your code is a violent psychopath who knows where you live.</p>\n    \n    <footer>\n      — John Woods,\n      <cite>Programming Wisdom</cite>\n    </footer>\n  </blockquote>\n  \n  <footer>\n    — Quoted by Jane Developer in\n    <cite>Best Practices Blog</cite>\n  </footer>\n</blockquote>\n\n// 4. Quote with multiple paragraphs\n<blockquote cite=\"https://example.com/long-quote\">\n  <p>First paragraph of the quoted content that establishes the main point and provides context for the discussion.</p>\n  \n  <p>Second paragraph continues the thought and expands on the initial premise with additional details and examples.</p>\n  \n  <p>Final paragraph concludes the quoted section and summarizes the key takeaways.</p>\n  \n  <footer>\n    — Book Author,\n    <cite>\n      <a href=\"https://example.com/long-quote\">Book Title</a>\n    </cite>\n  </footer>\n</blockquote>\n\n// 5. Styling semantic quotations\nblockquote {\n  border-left: 4px solid #4299e1;\n  margin: 1.5em 0;\n  padding: 0.5em 1em;\n  background: #f7fafc;\n  font-style: italic;\n}\n\nblockquote footer {\n  margin-top: 1em;\n  font-style: normal;\n  font-size: 0.9em;\n  color: #4a5568;\n}\n\nblockquote cite {\n  font-style: normal;\n}\n\nblockquote cite::before {\n  content: \"— \";\n}\n\nq {\n  quotes: \"\\201C\" \"\\201D\" \"\\2018\" \"\\2019\";\n}\n\nq::before {\n  content: open-quote;\n}\n\nq::after {\n  content: close-quote;\n}\n\n// 6. Enhanced with microdata\n<blockquote \n  itemscope \n  itemtype=\"https://schema.org/Quotation\"\n  cite=\"https://example.com/source\">\n  \n  <p itemprop=\"text\">\n    The most profound technologies are those that disappear.\n  </p>\n  \n  <footer>\n    — \n    <span itemprop=\"author\" itemscope itemtype=\"https://schema.org/Person\">\n      <span itemprop=\"name\">Mark Weiser</span>\n    </span>,\n    <cite itemprop=\"source\">The Computer for the 21st Century</cite>\n  </footer>\n</blockquote>\n\n// 7. JavaScript for interactive quotes\ndocument.addEventListener('DOMContentLoaded', function() {\n  // Add click handlers to citation links\n  document.querySelectorAll('blockquote cite a').forEach(link => {\n    link.addEventListener('click', function(e) {\n      e.preventDefault();\n      \n      const blockquote = this.closest('blockquote');\n      const sourceUrl = blockquote.getAttribute('cite');\n      \n      // Track quote clicks\n      if (window.gtag) {\n        gtag('event', 'quote_source_click', {\n          'event_category': 'Engagement',\n          'event_label': sourceUrl\n        });\n      }\n      \n      // Open source in new window\n      window.open(sourceUrl, '_blank');\n    });\n  });\n  \n  // Extract all quotes for analysis\n  const quotes = Array.from(document.querySelectorAll('blockquote')).map(quote => ({\n    text: quote.querySelector('p').textContent,\n    author: quote.querySelector('footer')?.textContent || 'Unknown',\n    source: quote.getAttribute('cite')\n  }));\n  \n  console.log('Page quotes:', quotes);\n});\n\n// 8. Responsive quote styling\n@media (max-width: 768px) {\n  blockquote {\n    margin: 1em 0;\n    padding: 0.5em;\n    font-size: 0.9em;\n  }\n  \n  blockquote footer {\n    font-size: 0.8em;\n  }\n}\n\n// 9. Print styles for quotes\n@media print {\n  blockquote {\n    border-left: 2px solid #000;\n    background: none;\n    page-break-inside: avoid;\n  }\n  \n  blockquote cite a::after {\n    content: \" (\" attr(href) \")\";\n    font-size: 0.8em;\n  }\n}[/CODE]\n\nUsing semantic elements for quotations and citations improves accessibility, SEO, and maintainability while providing clear structure for both human readers and automated systems.",
            "topic": {
                "topics": ["Semantic HTML5 Elements", "Microdata and Structured Data"]
            }
        },
        {
            "id": 23,
            "question": "What is the purpose of the <pre>preload</pre> and <pre>prefetch</pre> resource hints in this performance optimization?\n[CODE]<!-- Preload critical resources -->\n<link rel=\"preload\" href=\"critical-font.woff2\" as=\"font\" type=\"font/woff2\" crossorigin>\n<link rel=\"preload\" href=\"hero-image.webp\" as=\"image\" type=\"image/webp\">\n<link rel=\"preload\" href=\"main.js\" as=\"script\">\n\n<!-- Prefetch likely navigation targets -->\n<link rel=\"prefetch\" href=\"/products\" as=\"document\">\n<link rel=\"prefetch\" href=\"/about\" as=\"document\">\n\n<!-- Preconnect to third-party domains -->\n<link rel=\"preconnect\" href=\"https://fonts.googleapis.com\">\n<link rel=\"preconnect\" href=\"https://cdn.example.com\">\n\n<!-- DNS prefetch for additional domains -->\n<link rel=\"dns-prefetch\" href=\"https://analytics.example.com\">[/CODE]",
            "options": {
                "A": "They reduce JavaScript execution time",
                "B": "They eliminate render-blocking resources",
                "C": "They reduce resource loading latency",
                "D": "They compress HTML content"
            },
            "correct": "C",
            "explanation": "Resource hints like <pre>preload</pre>, <pre>prefetch</pre>, <pre>preconnect</pre>, and <pre>dns-prefetch</pre> reduce loading latency by telling the browser about important resources and connections before they're actually needed.\n\n[CODE]// Comprehensive resource hint strategy\n\n// 1. Preload - for critical resources needed soon\n<link \n  rel=\"preload\" \n  href=\"critical-style.css\" \n  as=\"style\" \n  onload=\"this.rel='stylesheet'\">\n\n<link \n  rel=\"preload\" \n  href=\"hero-image.avif\" \n  as=\"image\"\n  imagesrcset=\"hero-400.avif 400w, hero-800.avif 800w\"\n  imagesizes=\"100vw\">\n\n// 2. Prefetch - for likely future navigation\n<link \n  rel=\"prefetch\" \n  href=\"/next-page.html\" \n  as=\"document\"\n  crossorigin=\"anonymous\">\n\n// 3. Preconnect - for critical third-party origins\n<link \n  rel=\"preconnect\" \n  href=\"https://api.example.com\"\n  crossorigin>\n\n// 4. DNS-prefetch - for additional third-party domains\n<link \n  rel=\"dns-prefetch\" \n  href=\"https://cdnjs.cloudflare.com\">\n\n// Advanced preloading patterns\n\n// Conditional preloading based on user agent\n<script>\nif (window.innerWidth >= 768) {\n  // Preload desktop-specific resources\n  const link = document.createElement('link');\n  link.rel = 'preload';\n  link.href = '/desktop-hero.jpg';\n  link.as = 'image';\n  document.head.appendChild(link);\n} else {\n  // Preload mobile-specific resources\n  const link = document.createElement('link');\n  link.rel = 'preload';\n  link.href = '/mobile-hero.jpg';\n  link.as = 'image';\n  document.head.appendChild(link);\n}\n</script>\n\n// Dynamic preloading based on user interaction\ndocument.addEventListener('DOMContentLoaded', function() {\n  // Preload checkout page when user adds to cart\n  const addToCartButtons = document.querySelectorAll('.add-to-cart');\n  addToCartButtons.forEach(button => {\n    button.addEventListener('click', function() {\n      preloadResource('/checkout', 'document');\n    });\n  });\n  \n  // Preload search results when user focuses search\n  const searchInput = document.getElementById('search');\n  searchInput.addEventListener('focus', function() {\n    preloadResource('/search-results-template', 'fetch');\n  });\n});\n\nfunction preloadResource(url, as) {\n  const link = document.createElement('link');\n  link.rel = 'preload';\n  link.href = url;\n  link.as = as;\n  document.head.appendChild(link);\n}\n\n// Module preloading for JavaScript applications\n<link rel=\"modulepreload\" href=\"/src/main.js\">\n<link rel=\"modulepreload\" href=\"/src/utils.js\">\n<link rel=\"modulepreload\" href=\"/src/api.js\">\n\n// Responsive image preloading\n<link \n  rel=\"preload\"\n  href=\"hero-small.jpg\"\n  as=\"image\"\n  media=\"(max-width: 600px)\">\n\n<link \n  rel=\"preload\"\n  href=\"hero-large.jpg\"\n  as=\"image\"\n  media=\"(min-width: 601px)\">\n\n// Preloading with priority hints\n<link \n  rel=\"preload\" \n  href=\"critical-resource.js\" \n  as=\"script\"\n  fetchpriority=\"high\">\n\n// Monitoring preload effectiveness\nconst preloadObserver = new PerformanceObserver((list) => {\n  list.getEntries().forEach((entry) => {\n    if (entry.initiatorType === 'link') {\n      console.log(`Preloaded: ${entry.name} in ${entry.duration}ms`);\n      \n      // Track in analytics\n      if (window.gtag) {\n        gtag('event', 'preload_complete', {\n          'event_category': 'Performance',\n          'event_label': entry.name,\n          'value': Math.round(entry.duration)\n        });\n      }\n    }\n  });\n});\n\npreloadObserver.observe({entryTypes: ['resource']});\n\n// Fallback for browsers without preload support\nif (!document.createElement('link').relList.supports('preload')) {\n  // Implement alternative loading strategy\n  loadCriticalResources();\n}\n\nfunction loadCriticalResources() {\n  // Load critical CSS\n  const criticalCSS = document.createElement('link');\n  criticalCSS.rel = 'stylesheet';\n  criticalCSS.href = 'critical.css';\n  document.head.appendChild(criticalCSS);\n  \n  // Load critical fonts\n  const font = new FontFace('CriticalFont', 'url(critical-font.woff2)');\n  font.load().then(loadedFont => {\n    document.fonts.add(loadedFont);\n  });\n}\n\n// Service Worker integration for advanced caching\nif ('serviceWorker' in navigator) {\n  navigator.serviceWorker.addEventListener('message', event => {\n    if (event.data.type === 'RESOURCE_PRELOAD_REQUEST') {\n      // Preload resources via Service Worker\n      preloadResource(event.data.url, event.data.as);\n    }\n  });\n}[/CODE]\n\nStrategic use of resource hints can significantly improve perceived performance by reducing latency for critical resources and anticipating user navigation patterns.",
            "topic": {
                "topics": ["Performance Optimization", "Progressive Web App Features"]
            }
        },
        {
            "id": 24,
            "question": "What accessibility feature is provided by the <pre>aria-describedby</pre> attribute in this form validation?\n[CODE]<form>\n  <div>\n    <label for=\"username\">Username</label>\n    <input \n      type=\"text\" \n      id=\"username\" \n      name=\"username\"\n      required\n      aria-required=\"true\"\n      aria-invalid=\"false\"\n      aria-describedby=\"username-help username-error\">\n    \n    <div id=\"username-help\" class=\"help-text\">\n      Username must be 3-20 characters and can contain letters, numbers, and underscores.\n    </div>\n    \n    <div \n      id=\"username-error\" \n      class=\"error-message\" \n      role=\"alert\"\n      aria-live=\"polite\">\n    </div>\n  </div>\n  \n  <button type=\"submit\">Create Account</button>\n</form>\n\n<script>\nconst usernameInput = document.getElementById('username');\nconst usernameError = document.getElementById('username-error');\n\nusernameInput.addEventListener('blur', validateUsername);\nusernameInput.addEventListener('input', clearError);\n\nfunction validateUsername() {\n  const username = usernameInput.value.trim();\n  \n  if (username.length === 0) {\n    showError('Username is required');\n    return false;\n  }\n  \n  if (username.length < 3 || username.length > 20) {\n    showError('Username must be between 3 and 20 characters');\n    return false;\n  }\n  \n  if (!/^[a-zA-Z0-9_]+$/.test(username)) {\n    showError('Username can only contain letters, numbers, and underscores');\n    return false;\n  }\n  \n  clearError();\n  return true;\n}\n\nfunction showError(message) {\n  usernameInput.setAttribute('aria-invalid', 'true');\n  usernameError.textContent = message;\n}\n\nfunction clearError() {\n  usernameInput.setAttribute('aria-invalid', 'false');\n  usernameError.textContent = '';\n}\n</script>[/CODE]",
            "options": {
                "A": "It associates help text and error messages with the input for screen readers",
                "B": "It automatically validates the input value",
                "C": "It improves form submission performance",
                "D": "It enables custom CSS styling"
            },
            "correct": "A",
            "explanation": "The <pre>aria-describedby</pre> attribute associates both help text and error messages with the input element for screen readers. This ensures that users of assistive technologies receive both the initial instructions and any validation errors that occur.\n\n[CODE]// Comprehensive form accessibility implementation\n\n// 1. Enhanced form structure with full accessibility\n<form \n  novalidate \n  aria-labelledby=\"form-title\"\n  aria-describedby=\"form-instructions\">\n  \n  <h2 id=\"form-title\">Create New Account</h2>\n  \n  <div role=\"region\" aria-labelledby=\"form-instructions\">\n    <p id=\"form-instructions\" class=\"sr-only\">\n      All fields are required unless marked optional. \n      Error messages will be announced when they occur.\n    </p>\n  </div>\n  \n  <fieldset>\n    <legend>Account Information</legend>\n    \n    <div class=\"form-group\">\n      <label for=\"email\">\n        Email Address\n        <span class=\"required\" aria-hidden=\"true\">*</span>\n        <span class=\"sr-only\">required</span>\n      </label>\n      \n      <input \n        type=\"email\" \n        id=\"email\" \n        name=\"email\"\n        required\n        aria-required=\"true\"\n        autocomplete=\"email\"\n        aria-invalid=\"false\"\n        aria-describedby=\"email-help email-error\"\n        placeholder=\"your@email.com\">\n      \n      <div id=\"email-help\" class=\"help-text\">\n        We'll send a confirmation email to this address.\n      </div>\n      \n      <div \n        id=\"email-error\" \n        class=\"error-message\" \n        role=\"alert\"\n        aria-live=\"assertive\">\n      </div>\n    </div>\n    \n    <!-- Additional form fields -->\n  </fieldset>\n  \n  <div class=\"form-actions\">\n    <button type=\"submit\">Create Account</button>\n    <button type=\"button\" onclick=\"resetForm()\">Reset Form</button>\n  </div>\n</form>\n\n// 2. Advanced validation with accessibility\nclass AccessibleFormValidator {\n  constructor(formId) {\n    this.form = document.getElementById(formId);\n    this.fields = {};\n    \n    this.init();\n  }\n  \n  init() {\n    // Find all fields with validation\n    this.form.querySelectorAll('[aria-describedby]').forEach(field => {\n      const describedBy = field.getAttribute('aria-describedby');\n      const ids = describedBy.split(' ');\n      \n      this.fields[field.id] = {\n        element: field,\n        helpId: ids.find(id => id.includes('help')),\n        errorId: ids.find(id => id.includes('error')),\n        valid: false\n      };\n      \n      this.setupFieldValidation(field.id);\n    });\n    \n    // Form submission handling\n    this.form.addEventListener('submit', this.handleSubmit.bind(this));\n  }\n  \n  setupFieldValidation(fieldId) {\n    const field = this.fields[fieldId];\n    \n    field.element.addEventListener('blur', () => {\n      this.validateField(fieldId);\n    });\n    \n    field.element.addEventListener('input', () => {\n      this.clearFieldError(fieldId);\n    });\n    \n    field.element.addEventListener('invalid', (e) => {\n      e.preventDefault(); // Prevent browser validation UI\n      this.validateField(fieldId);\n    });\n  }\n  \n  validateField(fieldId) {\n    const field = this.fields[fieldId];\n    const value = field.element.value.trim();\n    \n    // Clear previous state\n    this.clearFieldError(fieldId);\n    \n    // Required field validation\n    if (field.element.required && value === '') {\n      this.showFieldError(fieldId, 'This field is required');\n      return false;\n    }\n    \n    // Type-specific validation\n    const isValid = this.performValidation(field.element, value);\n    \n    if (!isValid) {\n      this.showFieldError(fieldId, this.getValidationMessage(field.element));\n      return false;\n    }\n    \n    // Field is valid\n    field.valid = true;\n    field.element.setAttribute('aria-invalid', 'false');\n    \n    // Announce success to screen readers if previously invalid\n    if (field.element.getAttribute('data-was-invalid') === 'true') {\n      this.announceToScreenReader(`${field.element.labels[0]?.textContent} is now valid`);\n      field.element.removeAttribute('data-was-invalid');\n    }\n    \n    return true;\n  }\n  \n  showFieldError(fieldId, message) {\n    const field = this.fields[fieldId];\n    \n    field.element.setAttribute('aria-invalid', 'true');\n    field.element.setAttribute('data-was-invalid', 'true');\n    \n    const errorElement = document.getElementById(field.errorId);\n    if (errorElement) {\n      errorElement.textContent = message;\n      errorElement.style.display = 'block';\n    }\n    \n    // Focus the field for correction\n    field.element.focus();\n    \n    // Announce error to screen readers\n    this.announceToScreenReader(message);\n  }\n  \n  clearFieldError(fieldId) {\n    const field = this.fields[fieldId];\n    const errorElement = document.getElementById(field.errorId);\n    \n    if (errorElement) {\n      errorElement.textContent = '';\n      errorElement.style.display = 'none';\n    }\n  }\n  \n  performValidation(element, value) {\n    if (value === '') return true; // Empty optional fields are valid\n    \n    switch(element.type) {\n      case 'email':\n        return /^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$/.test(value);\n      \n      case 'tel':\n        return /^[\\d\\s\\-\\(\\)\\+]+$/.test(value);\n      \n      case 'url':\n        try {\n          new URL(value);\n          return true;\n        } catch {\n          return false;\n        }\n      \n      default:\n        // Check pattern attribute\n        if (element.pattern) {\n          const regex = new RegExp(element.pattern);\n          return regex.test(value);\n        }\n        \n        return true;\n    }\n  }\n  \n  getValidationMessage(element) {\n    // Use custom validation message or default\n    return element.dataset.validationMessage || \n           this.getDefaultValidationMessage(element);\n  }\n  \n  getDefaultValidationMessage(element) {\n    switch(element.type) {\n      case 'email':\n        return 'Please enter a valid email address';\n      case 'tel':\n        return 'Please enter a valid phone number';\n      case 'url':\n        return 'Please enter a valid URL';\n      default:\n        return 'Please check this field';\n    }\n  }\n  \n  announceToScreenReader(message) {\n    const announcer = document.getElementById('aria-live-announcer') ||\n      this.createLiveRegion();\n    \n    announcer.textContent = message;\n  }\n  \n  createLiveRegion() {\n    const div = document.createElement('div');\n    div.id = 'aria-live-announcer';\n    div.setAttribute('aria-live', 'polite');\n    div.setAttribute('aria-atomic', 'true');\n    div.className = 'sr-only';\n    document.body.appendChild(div);\n    return div;\n  }\n  \n  handleSubmit(e) {\n    e.preventDefault();\n    \n    // Validate all fields\n    let allValid = true;\n    Object.keys(this.fields).forEach(fieldId => {\n      if (!this.validateField(fieldId)) {\n        allValid = false;\n      }\n    });\n    \n    if (allValid) {\n      this.submitForm();\n    } else {\n      // Focus first invalid field\n      const firstInvalid = Object.values(this.fields)\n        .find(field => field.element.getAttribute('aria-invalid') === 'true');\n      \n      if (firstInvalid) {\n        firstInvalid.element.focus();\n      }\n    }\n  }\n  \n  submitForm() {\n    // Form is valid, proceed with submission\n    const formData = new FormData(this.form);\n    \n    // Show loading state\n    this.setFormState('loading');\n    \n    fetch(this.form.action, {\n      method: 'POST',\n      body: formData\n    })\n    .then(response => response.json())\n    .then(data => {\n      this.setFormState('success');\n      this.announceToScreenReader('Form submitted successfully');\n    })\n    .catch(error => {\n      this.setFormState('error');\n      this.announceToScreenReader('Form submission failed');\n    });\n  }\n  \n  setFormState(state) {\n    this.form.setAttribute('data-state', state);\n    \n    // Update ARIA attributes based on state\n    switch(state) {\n      case 'loading':\n        this.form.setAttribute('aria-busy', 'true');\n        break;\n      case 'success':\n      case 'error':\n        this.form.setAttribute('aria-busy', 'false');\n        break;\n    }\n  }\n}\n\n// Initialize form validator\ndocument.addEventListener('DOMContentLoaded', () => {\n  new AccessibleFormValidator('account-form');\n});[/CODE]\n\nProper use of ARIA attributes like <pre>aria-describedby</pre> creates a comprehensive accessibility experience that helps all users understand form requirements, receive validation feedback, and successfully complete forms.",
            "topic": {
                "topics": ["Accessibility (ARIA, Screen Readers)", "Form Validation and Constraints"]
            }
        },
        {
            "id": 25,
            "question": "What is the purpose of the <pre>decoding</pre> and <pre>fetchpriority</pre> attributes in this image optimization?\n[CODE]<img \n  src=\"hero-image.avif\"\n  alt=\"Product hero image\"\n  width=\"1200\"\n  height=\"630\"\n  loading=\"eager\"\n  decoding=\"async\"\n  fetchpriority=\"high\"\n  sizes=\"(max-width: 768px) 100vw, 50vw\"\n  srcset=\"\n    hero-400.avif 400w,\n    hero-800.avif 800w,\n    hero-1200.avif 1200w\n  \">\n\n<img \n  src=\"decoration.png\"\n  alt=\"Decorative element\"\n  width=\"200\"\n  height=\"200\"\n  loading=\"lazy\"\n  decoding=\"async\"\n  fetchpriority=\"low\"\n  importance=\"low\">[/CODE]",
            "options": {
                "A": "They control image decoding and loading priority for performance",
                "B": "They automatically compress images",
                "C": "They enable responsive image behavior",
                "D": "They improve image accessibility"
            },
            "correct": "A",
            "explanation": "The <pre>decoding</pre> and <pre>fetchpriority</pre> attributes control image decoding behavior and loading priority to optimize performance. <pre>decoding</pre> specifies whether image decoding should be synchronous or asynchronous, while <pre>fetchpriority</pre> indicates the relative priority of resource fetching.\n\n[CODE]// Comprehensive image optimization with modern attributes\n\n// 1. Critical above-the-fold image (high priority)\n<img \n  src=\"hero-image.avif\"\n  alt=\"Main product showcase\"\n  width=\"1600\"\n  height=\"900\"\n  loading=\"eager\"           // Load immediately\n  decoding=\"async\"          // Non-blocking decoding\n  fetchpriority=\"high\"      // High fetch priority\n  sizes=\"100vw\"\n  srcset=\"\n    hero-400.avif 400w,\n    hero-800.avif 800w,\n    hero-1200.avif 1200w,\n    hero-1600.avif 1600w\n  \">\n\n// 2. Decorative image (low priority)\n<img \n  src=\"background-pattern.png\"\n  alt=\"\"                    // Empty alt for decorative images\n  width=\"300\"\n  height=\"300\"\n  loading=\"lazy\"           // Defer loading\n  decoding=\"async\"         // Non-blocking decoding\n  fetchpriority=\"low\"      // Low fetch priority\n  role=\"presentation\"      // Explicitly decorative\n  aria-hidden=\"true\">      // Hide from screen readers\n\n// 3. Product gallery image (medium priority)\n<img \n  src=\"product-1.webp\"\n  alt=\"Product shown from front angle\"\n  width=\"600\"\n  height=\"400\"\n  loading=\"lazy\"\n  decoding=\"async\"\n  fetchpriority=\"auto\"     // Browser decides priority\n  sizes=\"(max-width: 768px) 50vw, 25vw\"\n  srcset=\"\n    product-1-300.webp 300w,\n    product-1-600.webp 600w,\n    product-1-900.webp 900w\n  \">\n\n// 4. Avatar image (low priority, but above fold)\n<img \n  src=\"user-avatar.jpg\"\n  alt=\"User profile picture\"\n  width=\"80\"\n  height=\"80\"\n  loading=\"eager\"         // Load immediately (small file)\n  decoding=\"async\"\n  fetchpriority=\"low\"\n  style=\"border-radius: 50%;\">\n\n// Advanced optimization techniques\n\n// Dynamic priority adjustment based on viewport\nfunction optimizeImagePriorities() {\n  const images = document.querySelectorAll('img[data-priority-viewport]');\n  \n  images.forEach(img => {\n    const viewport = img.dataset.priorityViewport;\n    const isInViewport = window.innerWidth >= parseInt(viewport);\n    \n    if (isInViewport) {\n      img.loading = 'eager';\n      img.fetchpriority = 'high';\n    } else {\n      img.loading = 'lazy';\n      img.fetchpriority = 'low';\n    }\n  });\n}\n\n// Usage\n<img \n  src=\"responsive-hero.jpg\"\n  alt=\"Responsive hero\"\n  data-priority-viewport=\"1024\"\n  width=\"1200\" height=\"600\">\n\n// Programmatic image optimization\nclass ImageOptimizer {\n  constructor() {\n    this.observer = null;\n    this.init();\n  }\n  \n  init() {\n    // Observe images for intersection and connection changes\n    this.observer = new IntersectionObserver(this.handleIntersection.bind(this), {\n      rootMargin: '50px 0px',\n      threshold: 0.1\n    });\n    \n    this.observeImages();\n    this.setupConnectionAwareLoading();\n  }\n  \n  observeImages() {\n    document.querySelectorAll('img[data-src]').forEach(img => {\n      this.observer.observe(img);\n    });\n  }\n  \n  handleIntersection(entries) {\n    entries.forEach(entry => {\n      if (entry.isIntersecting) {\n        const img = entry.target;\n        this.loadImage(img);\n        this.observer.unobserve(img);\n      }\n    });\n  }\n  \n  loadImage(img) {\n    const src = img.dataset.src;\n    \n    // Set src to trigger loading\n    img.src = src;\n    img.removeAttribute('data-src');\n    \n    // Apply optimal attributes based on image role\n    this.optimizeImageAttributes(img);\n    \n    // Track loading performance\n    this.trackImageLoad(img);\n  }\n  \n  optimizeImageAttributes(img) {\n    // Determine image role and set appropriate attributes\n    const role = this.determineImageRole(img);\n    \n    switch(role) {\n      case 'hero':\n        img.loading = 'eager';\n        img.fetchpriority = 'high';\n        img.decoding = 'async';\n        break;\n        \n      case 'content':\n        img.loading = 'lazy';\n        img.fetchpriority = 'auto';\n        img.decoding = 'async';\n        break;\n        \n      case 'decorative':\n        img.loading = 'lazy';\n        img.fetchpriority = 'low';\n        img.decoding = 'async';\n        \n        // Ensure decorative images are accessible\n        if (!img.alt) {\n          img.alt = '';\n          img.setAttribute('role', 'presentation');\n          img.setAttribute('aria-hidden', 'true');\n        }\n        break;\n    }\n  }\n  \n  determineImageRole(img) {\n    // Heuristic to determine image importance\n    if (img.classList.contains('hero') || img.offsetWidth >= 800) {\n      return 'hero';\n    }\n    \n    if (img.alt && img.alt !== '') {\n      return 'content';\n    }\n    \n    return 'decorative';\n  }\n  \n  setupConnectionAwareLoading() {\n    // Adjust loading strategy based on connection\n    if (navigator.connection) {\n      navigator.connection.addEventListener('change', () => {\n        this.adjustForConnection();\n      });\n    }\n  }\n  \n  adjustForConnection() {\n    const connection = navigator.connection;\n    const images = document.querySelectorAll('img');\n    \n    images.forEach(img => {\n      if (connection.saveData || connection.effectiveType === 'slow-2g') {\n        // Conservative loading on slow connections\n        img.loading = 'lazy';\n        img.fetchpriority = 'low';\n      } else {\n        // More aggressive loading on fast connections\n        if (this.isAboveTheFold(img)) {\n          img.loading = 'eager';\n          img.fetchpriority = 'high';\n        }\n      }\n    });\n  }\n  \n  isAboveTheFold(element) {\n    const rect = element.getBoundingClientRect();\n    return rect.top < window.innerHeight && rect.bottom > 0;\n  }\n  \n  trackImageLoad(img) {\n    const startTime = performance.now();\n    \n    img.addEventListener('load', () => {\n      const loadTime = performance.now() - startTime;\n      \n      console.log(`Image loaded: ${img.src} in ${loadTime}ms`);\n      \n      // Send to analytics\n      if (window.gtag) {\n        gtag('event', 'image_load', {\n          'event_category': 'Performance',\n          'event_label': img.src,\n          'value': Math.round(loadTime),\n          'non_interaction': true\n        });\n      }\n    });\n    \n    img.addEventListener('error', () => {\n      console.error(`Failed to load image: ${img.src}`);\n      \n      // Implement fallback strategy\n      this.handleImageError(img);\n    });\n  }\n  \n  handleImageError(img) {\n    // Try fallback formats\n    const src = img.src;\n    \n    if (src.endsWith('.avif')) {\n      img.src = src.replace('.avif', '.webp');\n    } else if (src.endsWith('.webp')) {\n      img.src = src.replace('.webp', '.jpg');\n    }\n    \n    // Or show placeholder\n    img.src = '/placeholder.jpg';\n  }\n}\n\n// Initialize image optimizer\ndocument.addEventListener('DOMContentLoaded', () => {\n  new ImageOptimizer();\n});\n\n// CSS for optimized image loading\nimg {\n  /* Prevent layout shifts */\n  display: block;\n  max-width: 100%;\n  height: auto;\n}\n\n/* Loading state styles */\nimg[data-src] {\n  background: #f7fafc;\n  min-height: 100px;\n}\n\n/* Blur-in effect for progressive loading */\nimg {\n  filter: blur(0px);\n  transition: filter 0.3s ease-in-out;\n}\n\nimg[data-src] {\n  filter: blur(10px);\n}\n\n/* High priority image styling */\nimg[fetchpriority=\"high\"] {\n  /* Optional: Add visual indicator for development */\n  /* border: 2px solid green; */\n}\n\n/* Print styles */\n@media print {\n  img {\n    break-inside: avoid;\n  }\n  \n  img[loading=\"lazy\"] {\n    /* Ensure lazy images print */\n    display: block !important;\n  }\n}[/CODE]\n\nStrategic use of modern image attributes like <pre>decoding</pre> and <pre>fetchpriority</pre> significantly improves Core Web Vitals by optimizing loading behavior, reducing main thread blocking, and ensuring critical images load quickly while deferring non-critical ones.",
            "topic": {
                "topics": ["Performance Optimization", "Media Elements and APIs"]
            }
        }
    ]
};