import { describe } from 'vitest';

// Original tests were moved to `src/views/__tests__/ResultsView.test.ts`.
// Keep a skipped placeholder here so the test runner does not treat this
// file as an empty test suite (which Vitest reports as an error).
describe.skip( 'results-restore (moved)', () => { } );
